---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles.css
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03
  - FOUN-04

must_haves:
  truths:
    - "styles.css begins with a PRIMITIVES :root block then a SEMANTIC :root block — no other CSS appears before them"
    - "All headings (h1, h2, h3) render with fluid clamp() sizes and negative letter-spacing — no fixed font-size overrides remain"
    - "No old token names (--ink, --charcoal, --graphite, --lime, --cyan, --coral, --paper, --white, --line, --muted, --shadow) appear anywhere in styles.css after the :root blocks"
    - "The floating header has backdrop-filter on .site-header and transform on .header-positioner — never on the same element"
    - "The .btn-primary button is a violet pill (background: var(--color-accent), border-radius: var(--radius-full))"
  artifacts:
    - path: "styles.css"
      provides: "Complete design token system + typography + header CSS split"
      contains: "--violet-600: #7c5cfc"
    - path: "styles.css"
      provides: "Two-layer token block"
      contains: "--color-accent: var(--violet-600)"
    - path: "styles.css"
      provides: "Fluid type scale"
      contains: "clamp(2.5rem, 5vw + 1rem, 5rem)"
    - path: "styles.css"
      provides: "iOS Safari-safe header split"
      contains: ".header-positioner"
  key_links:
    - from: "styles.css .scrolled .site-header"
      to: "styles.css .header-positioner"
      via: "JS adds .scrolled class to .header-positioner"
      pattern: "\.scrolled .site-header"
    - from: "styles.css component rules"
      to: "styles.css :root semantic block"
      via: "var() references to semantic tokens only"
      pattern: "var\(--color-"
---

<objective>
Replace the entire design token system and typography scale in styles.css. This plan also fixes the iOS Safari backdrop-filter/transform bug in the header CSS and rewrites the primary button to the violet pill style.

Purpose: Every subsequent phase writes component CSS that references the semantic tokens defined here. A broken or incomplete token block causes silent visual regressions across all future phases. This plan must complete before any downstream CSS is written.

Output: A fully updated styles.css with (1) complete two-layer :root token block, (2) fluid clamp() type scale with negative letter-spacing, (3) iOS Safari-safe header CSS split, (4) violet pill .btn-primary, and (5) zero remaining references to any old token name.
</objective>

<execution_context>
@/Users/manikmalhotra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/manikmalhotra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- Current :root block (lines 1–13) — to be REPLACED entirely -->
<!-- Old token names that MUST NOT remain after this plan: -->
<!--   --ink, --charcoal, --graphite, --line, --muted, --paper, --white, --cyan, --lime, --coral, --shadow -->

<!-- Old-to-new migration map (apply to every reference in the file): -->
<!--   var(--ink)       → var(--color-bg) for backgrounds, var(--color-text-primary) for text -->
<!--   var(--charcoal)  → var(--color-surface-1) -->
<!--   var(--graphite)  → var(--color-surface-2) -->
<!--   var(--paper)     → var(--color-surface-2) -->
<!--   var(--white)     → #ffffff or var(--color-text-primary) -->
<!--   var(--line)      → var(--color-border) -->
<!--   var(--muted)     → var(--color-text-secondary) -->
<!--   var(--cyan)      → var(--color-cyan) (signal only — NOT for accent) -->
<!--   var(--lime)      → var(--color-accent) -->
<!--   var(--coral)     → var(--color-error) -->
<!--   var(--shadow)    → var(--shadow-card) -->

<!-- Current .site-header (lines 47–64) — iOS Safari bug: transform + backdrop-filter on same element -->
<!-- Current h1 (line ~202): font-size: 4.55rem; letter-spacing: 0 — REPLACE with clamp() -->
<!-- Current h2: font-size: 3rem; letter-spacing: 0 — REPLACE with clamp() -->
<!-- Current h3: font-size: 1.18rem — REPLACE with clamp() -->
<!-- Breakpoint font-size overrides at lines ~776–782 and ~809–815 — REMOVE (clamp() makes them redundant) -->
<!-- Current .btn-primary (lines ~275–283): lime background, pulse animation — REPLACE with violet pill -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace :root token block and sweep all old token references</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (read entire file — must know every occurrence of old token names before replacing the :root block)
    - .planning/phases/01-foundation/01-PATTERNS.md (section "Pattern Assignments — styles.css Token Block" for exact two-layer block)
    - .planning/phases/01-foundation/01-UI-SPEC.md (section "Color — Full Token Block" for complete semantic token list)
  </read_first>
  <action>
Step 1 — Replace lines 1–13 (the existing flat :root block) with the full two-layer block:

```css
/* === PRIMITIVES === */
:root {
  /* Violet scale */
  --violet-600:  #7c5cfc;
  --violet-400:  #9b7fff;
  --violet-dim:  rgba(124, 92, 252, 0.12);

  /* Zinc / grey scale */
  --gray-950:    #0a0a0b;
  --gray-900:    #111113;
  --gray-800:    #18181b;
  --gray-750:    #232326;

  /* Signal */
  --cyan-400:    #22d3ee;
  --green-400:   #4ade80;
  --amber-400:   #fbbf24;
  --red-400:     #f87171;

  /* Motion */
  --duration-fast: 150ms;
  --duration-base: 250ms;
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
}

/* === SEMANTIC === */
:root {
  /* Backgrounds */
  --color-bg:         var(--gray-950);
  --color-surface-1:  var(--gray-900);
  --color-surface-2:  var(--gray-800);
  --color-surface-3:  var(--gray-750);

  /* Borders */
  --color-border:        rgba(255,255,255,0.08);
  --color-border-strong: rgba(255,255,255,0.14);
  --color-border-hover:  rgba(255,255,255,0.16);
  --color-border-focus:  rgba(255,255,255,0.28);

  /* Text */
  --color-text-primary:   #f4f4f5;
  --color-text-secondary: #a1a1aa;
  --color-text-muted:     rgba(255,255,255,0.50);

  /* Accent */
  --color-accent:         var(--violet-600);
  --color-accent-dim:     var(--violet-dim);
  --color-accent-hover:   var(--violet-400);

  /* Signal */
  --color-error:          var(--red-400);
  --color-success:        var(--green-400);
  --color-cyan:           var(--cyan-400);

  /* Header glassmorphism */
  --header-bg-default:    rgba(8, 10, 10, 0.78);
  --header-bg-scrolled:   rgba(8, 10, 10, 0.92);

  /* Typography */
  --font-sans: "Inter", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-mono: "JetBrains Mono", "Fira Code", ui-monospace, "Courier New", monospace;
  --tracking-tight:   -0.04em;
  --tracking-heading: -0.03em;
  --tracking-label:    0.08em;

  /* Spacing */
  --space-1:  4px;
  --space-2:  8px;
  --space-3:  12px;
  --space-4:  16px;
  --space-5:  20px;
  --space-6:  24px;
  --space-8:  32px;
  --space-12: 48px;
  --space-16: 64px;
  --space-20: 80px;
  --space-24: 96px;

  /* Border radius */
  --radius-sm:   6px;
  --radius-md:   8px;
  --radius-lg:   12px;
  --radius-full: 9999px;

  /* Shadows */
  --shadow-card: 0 1px 0 rgba(255,255,255,0.06) inset, 0 24px 64px rgba(0,0,0,0.4);
  --shadow-glow: 0 0 0 1px var(--color-accent), 0 0 24px var(--color-accent-dim);

  /* Layout */
  --max-width: 1120px;
  --section-padding: var(--space-24) max(var(--space-6), calc((100% - var(--max-width)) / 2));
}
```

Step 2 — Update the body rule (lines 23–29) to:
```css
body {
  margin: 0;
  color: var(--color-text-primary);
  background: var(--color-bg);
  font-family: var(--font-sans);
  line-height: 1.5;
  font-optical-sizing: auto;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
```

Step 3 — Sweep the entire remainder of styles.css (every line below the :root blocks) and replace ALL occurrences of old token names using this exact mapping:
- `var(--ink)` → `var(--color-bg)` when used as a background value; `var(--color-text-primary)` when used as a text/color value. Check context: `.site-header { color: var(--ink) }` = text → color-text-primary; `background: var(--ink)` = background → color-bg.
- `var(--charcoal)` → `var(--color-surface-1)`
- `var(--graphite)` → `var(--color-surface-2)`
- `var(--paper)` → `var(--color-surface-2)` (was white/light card bg — now dark surface)
- `var(--white)` → `#ffffff` when used for pure white text/borders; `var(--color-text-primary)` when used as the main text color
- `var(--line)` → `var(--color-border)`
- `var(--muted)` → `var(--color-text-secondary)`
- `var(--cyan)` → `var(--color-cyan)`
- `var(--lime)` → `var(--color-accent)`
- `var(--coral)` → `var(--color-error)`
- `var(--shadow)` → `var(--shadow-card)`

Do NOT leave any old token name anywhere in the file. After the sweep, grep for each old name to confirm zero occurrences.
  </action>
  <verify>
    <automated>grep -v '^\s*/\*' styles.css | grep -c '\-\-ink\|\-\-charcoal\|\-\-graphite\|\-\-lime\|\-\-coral\|\-\-paper\|\-\-white\b\|\-\-line\b\|\-\-muted\b\|\-\-shadow\b' || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\-\-violet-600: #7c5cfc' styles.css` returns 1
    - `grep -c '\-\-color-accent: var(--violet-600)' styles.css` returns 1
    - `grep -c '\-\-color-bg:' styles.css` returns 1
    - `grep -c '\-\-color-surface-1:' styles.css` returns 1
    - `grep -c '\-\-header-bg-default:' styles.css` returns 1
    - `grep -c '\-\-radius-full: 9999px' styles.css` returns 1
    - `grep -c 'font-optical-sizing: auto' styles.css` returns 1
    - `grep -c '\-webkit-font-smoothing: antialiased' styles.css` returns 1
    - `grep -c 'var(--font-sans)' styles.css` returns at least 1 (body rule)
    - `grep -v '^\s*/\*' styles.css | grep -c 'var(--ink)\|var(--charcoal)\|var(--graphite)\|var(--lime)\|var(--coral)\|var(--paper)\|var(--white)\b\|var(--line)\b\|var(--muted)\b\|var(--shadow)\b'` returns 0 — no old token references remain outside comments
  </acceptance_criteria>
  <done>The two-layer :root block is in place, body rule is updated, and zero old token names remain anywhere in styles.css outside the :root declarations themselves.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Typography scale, header CSS split, btn-primary violet pill, nav-link active state</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (read in full — Task 1 has already updated it; must see current state before making further edits)
    - .planning/phases/01-foundation/01-PATTERNS.md (sections: "Typography Scale", "Header CSS Split", "Nav Link and CTA Button Rules")
    - .planning/phases/01-foundation/01-UI-SPEC.md (sections: "Typography Type Scale", "Component Specifications — Header", "Book a Call Button")
  </read_first>
  <action>
Step 1 — Replace the h1, h2, h3 rules (currently around line ~202–216) with clamp()-based fluid scale:
```css
h1 {
  max-width: 760px;
  font-size: clamp(2.5rem, 5vw + 1rem, 5rem);
  font-weight: 700;
  line-height: 1.02;
  letter-spacing: var(--tracking-tight);
}

h2 {
  max-width: 740px;
  font-size: clamp(1.75rem, 3vw + 0.75rem, 3rem);
  font-weight: 700;
  line-height: 1.06;
  letter-spacing: var(--tracking-heading);
}

h3 {
  font-size: clamp(1.25rem, 1.5vw + 0.5rem, 1.75rem);
  font-weight: 600;
  line-height: 1.3;
  letter-spacing: var(--tracking-heading);
}
```

Step 2 — Find and REMOVE the breakpoint-scoped h1/h2 font-size overrides in the media query blocks at the bottom of the file:
- Remove `h1 { font-size: 3.25rem; }` from the 940px block
- Remove `h2 { font-size: 2.35rem; }` from the 940px block
- Remove `h1 { font-size: 2.55rem; }` from the 640px block
- Remove `h2 { font-size: 2rem; }` from the 640px block

Step 3 — Replace the .site-header rule (the iOS Safari bug at lines ~47–64) with the wrapper split:
```css
/* OUTER: positioning only — NO backdrop-filter ever */
.header-positioner {
  position: fixed;
  top: 18px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 50;
  width: min(1120px, calc(100% - 32px));
}

/* INNER: all visual styles — NO transform ever */
.site-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 10px 12px;
  color: var(--color-text-primary);
  background: var(--header-bg-default);
  border: 1px solid var(--color-border-strong);
  border-radius: var(--radius-md);
  backdrop-filter: blur(18px) saturate(180%);
  -webkit-backdrop-filter: blur(18px) saturate(180%);
  transition: background var(--duration-base) var(--ease-out);
}

/* Scroll-darkened state: JS adds .scrolled to .header-positioner */
.scrolled .site-header {
  background: var(--header-bg-scrolled);
}
```

Step 4 — Replace the .btn-primary and .btn-primary:hover rules with the violet pill:
```css
.btn-primary {
  color: #ffffff;
  background: var(--color-accent);
  border-radius: var(--radius-full);
  padding: var(--space-2) var(--space-4);
  font-size: 0.875rem;
  font-weight: 600;
  border: none;
  cursor: pointer;
  transition: background var(--duration-fast) var(--ease-out),
              transform var(--duration-fast) var(--ease-out);
}

.btn-primary:hover {
  background: var(--color-accent-hover);
  transform: translateY(-1px);
}

.btn-primary:active {
  transform: translateY(0);
}
```
Also remove the `@keyframes pulse` animation if it exists and was only used by .btn-primary.

Step 5 — Update .site-nav a rules to use semantic tokens and add .nav-link active indicator. Find the .site-nav a rule and update to:
```css
.site-nav a,
.nav-link {
  padding: 9px 12px;
  color: var(--color-text-secondary);
  border-radius: var(--radius-md);
  font-size: 0.875rem;
  font-weight: 500;
  transition: color var(--duration-fast) var(--ease-out);
}

.site-nav a:hover,
.nav-link:hover,
.site-nav a:focus-visible,
.nav-link:focus-visible {
  color: var(--color-text-primary);
  background: rgba(255, 255, 255, 0.07);
}

/* Active section indicator: violet dot below active nav link */
.nav-link.is-active {
  color: var(--color-text-primary);
  position: relative;
}

.nav-link.is-active::after {
  content: "";
  position: absolute;
  bottom: 4px;
  left: 50%;
  transform: translateX(-50%);
  width: 4px;
  height: 4px;
  background: var(--color-accent);
  border-radius: var(--radius-full);
}
```
  </action>
  <verify>
    <automated>grep -c 'clamp(2.5rem, 5vw + 1rem, 5rem)' styles.css && grep -c '.header-positioner' styles.css && grep -c '-webkit-backdrop-filter' styles.css && grep -c 'var(--radius-full)' styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'clamp(2.5rem, 5vw + 1rem, 5rem)' styles.css` returns 1 (h1 fluid size)
    - `grep -c 'clamp(1.75rem, 3vw + 0.75rem, 3rem)' styles.css` returns 1 (h2 fluid size)
    - `grep -c 'clamp(1.25rem, 1.5vw + 0.5rem, 1.75rem)' styles.css` returns 1 (h3 fluid size)
    - `grep -c 'var(--tracking-tight)' styles.css` returns at least 1 (h1 letter-spacing)
    - `grep -c 'var(--tracking-heading)' styles.css` returns at least 2 (h2 and h3)
    - `grep -c '\.header-positioner' styles.css` returns at least 2 (the rule block + .scrolled descendant)
    - `grep -c 'transform: translateX(-50%)' styles.css` returns 1 — only in .header-positioner, NOT in .site-header
    - `grep -c 'backdrop-filter: blur(18px)' styles.css` returns 1 — only in .site-header, NOT in .header-positioner
    - `grep -c '\-webkit-backdrop-filter: blur(18px)' styles.css` returns 1
    - `grep -c '\.scrolled \.site-header' styles.css` returns 1
    - `grep -c 'background: var(--color-accent)' styles.css` returns at least 1 (btn-primary)
    - `grep -c 'border-radius: var(--radius-full)' styles.css` returns at least 1 (btn-primary)
    - `grep -c '\.nav-link\.is-active' styles.css` returns at least 1
    - `grep -c 'font-size: 4.55rem' styles.css` returns 0 — old fixed h1 size gone
    - `grep -c 'font-size: 3rem' styles.css` returns 0 — old fixed h2 size gone (unless used elsewhere for non-heading purpose)
  </acceptance_criteria>
  <done>Fluid type scale is in place with clamp() on all headings and negative letter-spacing via tokens. The .site-header no longer has transform on the same element as backdrop-filter. The header split is in CSS, btn-primary is a violet pill, and .nav-link.is-active shows a violet dot indicator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS custom properties | Resolved at paint time — no user input touches this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | styles.css token block | accept | Static file; no runtime injection; tokens are paint-time only; low-value target |
| T-01-02 | Denial of Service | Google Fonts CDN (Phase 1 adds link in Plan 02) | accept | Fonts fall back to system font stack if CDN unavailable; no functionality impact |
</threat_model>

<verification>
After both tasks complete, open styles.css in a browser via the local file (or `open index.html`) and confirm visually:
1. Page background is near-black (#0a0a0b) — not white or light grey
2. Any visible text uses the dark-on-light-or-vice-versa scheme with #f4f4f5 text on dark background
3. Any visible header renders as a floating pill with backdrop blur
4. Any visible button using .btn-primary shows violet fill (#7c5cfc), not lime green

Grep gates (run from project root):
```bash
grep -c '--violet-600: #7c5cfc' styles.css        # must be 1
grep -c '--color-accent: var(--violet-600)' styles.css  # must be 1
grep -c 'clamp(2.5rem' styles.css                  # must be 1
grep -c '.header-positioner' styles.css            # must be >= 2
grep -c '-webkit-backdrop-filter' styles.css       # must be 1
grep -v '^\s*/\*' styles.css | grep 'var(--ink)\|var(--lime)\|var(--cyan)\b\|var(--charcoal)' # must return empty
```
</verification>

<success_criteria>
- styles.css begins with PRIMITIVES :root block, immediately followed by SEMANTIC :root block, with no other CSS rules before them
- Two-layer block contains: --violet-600: #7c5cfc, --color-accent: var(--violet-600), --color-bg: var(--gray-950), all three surface levels, all four border tokens, all three text tokens, header glassmorphism tokens, all spacing tokens from --space-1 through --space-24, all four radius tokens
- body rule: color: var(--color-text-primary), background: var(--color-bg), font-family: var(--font-sans), font-optical-sizing: auto, -webkit-font-smoothing: antialiased
- h1: font-size: clamp(2.5rem, 5vw + 1rem, 5rem), letter-spacing: var(--tracking-tight)
- h2: font-size: clamp(1.75rem, 3vw + 0.75rem, 3rem), letter-spacing: var(--tracking-heading)
- h3: font-size: clamp(1.25rem, 1.5vw + 0.5rem, 1.75rem), letter-spacing: var(--tracking-heading)
- No breakpoint-scoped h1/h2 font-size overrides remain in any @media block
- .header-positioner has position: fixed, transform: translateX(-50%), NO backdrop-filter
- .site-header has backdrop-filter: blur(18px) saturate(180%), -webkit-backdrop-filter, NO transform
- .scrolled .site-header rule exists with background: var(--header-bg-scrolled)
- .btn-primary has background: var(--color-accent), border-radius: var(--radius-full), color: #ffffff
- .nav-link.is-active rule exists with violet dot ::after pseudo-element
- Zero occurrences of old token names (--ink, --charcoal, --graphite, --lime, --cyan, --coral, --paper, --white, --line, --muted, --shadow) anywhere in component CSS below the :root blocks
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template at `@/Users/manikmalhotra/.claude/get-shit-done/templates/summary.md`.
</output>
