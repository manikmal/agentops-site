---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - FOUN-03
  - FOUN-05
  - HEAD-01
  - HEAD-02
  - HEAD-03

must_haves:
  truths:
    - "Opening index.html shows Inter loading via Google Fonts preconnect + stylesheet link in <head>"
    - "The header is a floating pill: outer .header-positioner wrapper contains inner <header class='site-header'>, with data-header on the wrapper"
    - "Nav labels read: Services / Work / Process / About / Contact — with .nav-link class on each anchor"
    - "A 'Book a call' button (.btn-primary.btn-book-call) is visible at the right end of the nav"
    - "The hero H1 reads 'Build AI products that scale.' and the lede reads 'End-to-end AI engineering for founders who need it done right.'"
    - "No phone number, no Gmail address in hero, no hero-metrics block, no packages section anywhere in the HTML"
    - "No SME-era strings remain: no 'automation audit', no 'SMEs', no 'We build', no 'small and medium businesses'"
  artifacts:
    - path: "index.html"
      provides: "Google Fonts Inter variable font loading"
      contains: "fonts.googleapis.com/css2?family=Inter"
    - path: "index.html"
      provides: "iOS Safari-safe header wrapper"
      contains: "header-positioner"
    - path: "index.html"
      provides: "Startup-native hero copy"
      contains: "Build AI products that scale."
    - path: "index.html"
      provides: "Startup-native title and meta"
      contains: "AI Engineering for Founders"
  key_links:
    - from: "index.html .header-positioner[data-header]"
      to: "script.js querySelector('[data-header]')"
      via: "data-header attribute on outer wrapper — JS scroll handler targets this"
      pattern: "data-header"
    - from: "index.html .nav-link[href='#agents']"
      to: "script.js sectionObserver"
      via: ".nav-link class required for active tracking IntersectionObserver (Plan 03)"
      pattern: "class=\"nav-link\""
---

<objective>
Update index.html with Inter font loading, the iOS Safari-safe header HTML restructure, startup-native nav labels and CTA button, hero copy replacements, and complete SME language audit including removal of hero-metrics, packages section, contact phone/Gmail, and footer phone link.

Purpose: This plan establishes the HTML structural shell that Plan 03 (JS) depends on — specifically the .header-positioner wrapper with data-header and the .nav-link classes on anchors. It also satisfies the FOUN-05 language audit requirement.

Output: An updated index.html where (1) Inter loads correctly, (2) the header uses the wrapper split pattern, (3) nav shows startup labels with "Book a call" CTA, (4) hero copy is startup-native, and (5) all SME-era copy and structural SME elements are removed.
</objective>

<execution_context>
@/Users/manikmalhotra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/manikmalhotra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- Current <head> (lines 1–20): no font loading tags; title and meta description contain SME-era copy -->
<!-- Current header (lines 22–35): single <header class="site-header" data-header> — must split into wrapper + inner -->
<!-- Current nav labels: AI Agents / Automations / DevOps / Portfolio / Contact -->
<!-- No "Book a call" CTA button exists in header -->
<!-- Hero h1 (line ~45): "Make your business run like a smarter system." -->
<!-- Hero lede (line ~47): "We build AI assistants...small and medium businesses." -->
<!-- .hero-contact-links div contains Gmail and phone — REMOVE entirely -->
<!-- .hero-metrics div — REMOVE entirely -->
<!-- .ops-console flow rows: SME content (CRM, Instagram) — REPLACE with startup-native agent-trace -->
<!-- <section class="section packages-section"> (lines ~300–322) — REMOVE entire section -->
<!-- contact section .direct-contact div: Gmail + phone — REMOVE -->
<!-- contact form phone/WhatsApp field — REMOVE -->
<!-- contact form textarea placeholder: WhatsApp example — REPLACE -->
<!-- Footer phone link <a href="tel:..."> — REMOVE -->

<!-- CRITICAL: data-header must end up on .header-positioner (the outer div), NOT on .site-header -->
<!-- CRITICAL: nav anchors must have class="nav-link" — required by Plan 03 IntersectionObserver -->
<!-- Nav href values: keep existing anchors (#agents, #automations, #devops, #portfolio, #contact) -->
<!--   Only the visible link TEXT changes (not the href): -->
<!--   #agents → "Services", #automations → "Work", #devops → "Process", #portfolio → "About", #contact → "Contact" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update &lt;head&gt; and restructure header HTML</name>
  <files>index.html</files>
  <read_first>
    - index.html (read lines 1–40 to see exact current state of head and header)
    - .planning/phases/01-foundation/01-PATTERNS.md (section "index.html — Head Section" and "index.html — Header Restructure")
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-05, D-07, D-08, D-12)
  </read_first>
  <action>
Step 1 — Update &lt;title&gt; to:
```
AgentOps Studio | AI Engineering for Founders &amp; Growing Teams
```

Step 2 — Update &lt;meta name="description"&gt; content attribute to:
```
End-to-end AI product builds, agent orchestration, and DevOps for startups and growing businesses.
```

Step 3 — In &lt;head&gt;, find the existing `&lt;link rel="preconnect" href="https://images.unsplash.com"&gt;` and REMOVE it (no longer needed). Then add the three Inter font tags BEFORE the `&lt;link rel="stylesheet" href="styles.css"&gt;` line:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap" rel="stylesheet">
```

Step 4 — Replace the header block (currently `&lt;header class="site-header" data-header&gt;...&lt;/header&gt;`) with the wrapper split structure. The data-header attribute moves to the outer div:
```html
<div class="header-positioner" data-header>
  <header class="site-header">
    <a class="brand" href="#top" aria-label="AgentOps Studio home">
      <img src="favicon.svg" alt="" class="brand-icon" aria-hidden="true" width="36" height="36">
      <img src="wordmark.png" alt="AgentOps Studio" class="brand-wordmark">
    </a>
    <button class="nav-toggle" data-nav-toggle aria-expanded="false" aria-controls="site-nav">Menu</button>
    <nav class="site-nav" id="site-nav" data-nav>
      <a class="nav-link" href="#agents">Services</a>
      <a class="nav-link" href="#automations">Work</a>
      <a class="nav-link" href="#devops">Process</a>
      <a class="nav-link" href="#portfolio">About</a>
      <a class="nav-link" href="#contact">Contact</a>
    </nav>
    <a class="btn-primary btn-book-call" href="#contact">Book a call</a>
  </header>
</div>
```

Note: href anchors are preserved as-is (#agents, #automations, #devops, #portfolio, #contact). Only the visible link TEXT is updated. The section id attributes on the body sections are NOT changed in this phase.
  </action>
  <verify>
    <automated>grep -c 'fonts.googleapis.com/css2?family=Inter' index.html && grep -c 'header-positioner' index.html && grep -c 'data-header' index.html && grep -c 'btn-book-call' index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AI Engineering for Founders' index.html` returns 1 (title updated)
    - `grep -c 'End-to-end AI product builds, agent orchestration' index.html` returns 1 (meta description updated)
    - `grep -c 'fonts.googleapis.com' index.html` returns at least 2 (preconnect + stylesheet link)
    - `grep -c 'fonts.gstatic.com' index.html` returns 1 (preconnect with crossorigin)
    - `grep -c 'family=Inter' index.html` returns 1 (Inter variable font link)
    - `grep -c 'images.unsplash.com' index.html` returns 0 (removed)
    - `grep -c 'class="header-positioner" data-header' index.html` returns 1 (outer wrapper has data-header)
    - `grep -c 'class="site-header"' index.html` returns 1 — and it must NOT contain data-header (verify by reading the line)
    - `grep -c 'class="nav-link"' index.html` returns 5 (all five nav links have .nav-link class)
    - `grep -c 'btn-book-call' index.html` returns 1 ("Book a call" CTA button present)
    - `grep -c 'href="#contact">Book a call' index.html` returns 1
    - `grep -c 'AI Agents\|Automations\|DevOps\|Portfolio' index.html` returns 0 (old nav labels removed from nav; note: section IDs #agents etc. still exist on sections — only nav text changed)
  </acceptance_criteria>
  <done>Head has Inter font loading with preconnect tags. Title and meta description are startup-native. Header is restructured as .header-positioner[data-header] wrapping .site-header, with nav-link classes on all anchors and "Book a call" CTA button at the right end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hero copy, SME language audit, and structural element removal</name>
  <files>index.html</files>
  <read_first>
    - index.html (read lines 38–376 to see full body — hero section, ops console, packages section, contact section, footer)
    - .planning/phases/01-foundation/01-PATTERNS.md (sections: "Hero Section Language Audit", "Ops Console Placeholder Copy", "Packages Section Removal", "Contact Section Cleanup", "Footer Cleanup")
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-11, D-12, D-13, D-14)
    - .planning/phases/01-foundation/01-UI-SPEC.md (section "SME Language Audit Checklist")
  </read_first>
  <action>
Step 1 — Hero copy replacements:
- Find the `.eyebrow` paragraph in the hero section that contains "AI agents + automations + DevOps for SMEs" — REMOVE the entire element.
- Replace the `&lt;h1&gt;` text: "Make your business run like a smarter system." → "Build AI products that scale."
- Replace the `.hero-lede` paragraph text: "We build AI assistants, social media automations, internal tools, and reliable deployment pipelines for growing small and medium businesses." → "End-to-end AI engineering for founders who need it done right."
- Update the primary CTA link text: "Book an automation audit" → "Book a discovery call"
- Update the secondary CTA link text: "View portfolio" → "View work"

Step 2 — Remove structural SME elements from hero:
- Remove the entire `.hero-contact-links` div (the div containing Gmail address and phone number links). The entire element from opening `&lt;div class="hero-contact-links"` to its closing `&lt;/div&gt;` must be deleted.
- Remove the entire `.hero-metrics` div (the counter/metrics block). The entire element from opening `&lt;div class="hero-metrics"` to its closing `&lt;/div&gt;` must be deleted.

Step 3 — Replace the ops-console flow rows (inside `.ops-console` or equivalent class that holds the SME agent-trace content). Replace ALL existing flow-row elements with these startup-native placeholders:
```html
<div class="flow-row active">
  <span class="flow-dot"></span>
  <div>
    <strong>Orchestrator</strong>
    <p>Deployment pipeline triggered on merge to main.</p>
  </div>
  <em>2s</em>
</div>
<div class="flow-row">
  <span class="flow-dot"></span>
  <div>
    <strong>Data Agent</strong>
    <p>User event batch ingested and routed to analytics sink.</p>
  </div>
  <em>8s</em>
</div>
<div class="flow-row">
  <span class="flow-dot"></span>
  <div>
    <strong>Monitoring Agent</strong>
    <p>Latency spike detected — alert dispatched to on-call.</p>
  </div>
  <em>1s</em>
</div>
```

Step 4 — Remove the packages section entirely. Delete the entire `&lt;section class="section packages-section"&gt;...&lt;/section&gt;` block from the HTML. Note: any associated CSS rules (.packages-section, .package-card, .package-grid) should also be removed from styles.css at this step to avoid dead code.

Step 5 — Contact section cleanup (partial — Phase 4 will rebuild this section):
- Remove the `.direct-contact` div (contains Gmail address + phone + LinkedIn).
- Remove the phone/WhatsApp `&lt;label&gt;&lt;input name="phone"&gt;&lt;/label&gt;` field from the contact form.
- Update the contact form `&lt;textarea&gt;` placeholder attribute from "Example: We manually follow up with every unpaid invoice on WhatsApp." to "Example: We need an AI agent to handle onboarding flows for new signups."

Step 6 — Footer cleanup:
- In the site footer, find the phone link `&lt;a href="tel:+919599668843"&gt;Phone&lt;/a&gt;` and REMOVE it. Keep the Gmail email link and LinkedIn link (Phase 4 will replace Gmail with branded email).

Step 7 — Additional SME language audit (h2 headings in section headers that are clearly SME-era):
- Find any `&lt;h2&gt;` or eyebrow text that still reads "SMEs" or "for SMEs" or "We turn scattered" or uses "we" as first-person plural voice — update to remove or neutralize. Focus on h2 headings and eyebrow elements in the agents/automations/social/devops sections that are single-line fixes. Do NOT rewrite full body copy paragraphs (those are Phase 3 scope). For example, if an eyebrow reads "DevOps for SMEs" change it to "DevOps" or "For Founders & Teams"; if an h2 reads "Agents for the daily work SMEs keep postponing" change it to "Agents for the daily work that slows you down".
  </action>
  <verify>
    <automated>grep -c 'automation audit\|SMEs\|small and medium\|We build\|hero-contact-links\|hero-metrics\|packages-section' index.html || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ic 'automation audit' index.html` returns 0 (removed from CTA and any other occurrence)
    - `grep -ic 'SMEs' index.html` returns 0 (all "SMEs" strings removed from visible copy)
    - `grep -ic 'small and medium businesses' index.html` returns 0
    - `grep -ic 'We build' index.html` returns 0 (first-person plural removed)
    - `grep -c 'hero-contact-links' index.html` returns 0 (div removed)
    - `grep -c 'hero-metrics' index.html` returns 0 (div removed)
    - `grep -c 'packages-section' index.html` returns 0 (section removed)
    - `grep -c 'Build AI products that scale.' index.html` returns 1 (new H1)
    - `grep -c 'End-to-end AI engineering for founders' index.html` returns 1 (new lede)
    - `grep -c 'Book a discovery call' index.html` returns 1 (new primary CTA)
    - `grep -c 'View work' index.html` returns 1 (new secondary CTA)
    - `grep -c 'href="tel:' index.html` returns 0 (phone links removed)
    - `grep -c 'name="phone"' index.html` returns 0 (phone form field removed)
    - `grep -c 'manikmalhotra6@gmail.com' index.html` returns at most 1 (only in footer; removed from hero and contact section direct-contact block)
    - `grep -c 'Orchestrator' index.html` returns 1 (startup-native agent trace present)
    - `grep -c 'Deployment pipeline triggered' index.html` returns 1 (startup-native flow row)
  </acceptance_criteria>
  <done>Hero shows startup-native copy. All structural SME elements (hero-contact-links, hero-metrics, packages section) are removed. Contact section no longer has Gmail/phone in the direct-contact block or a phone form field. Footer has no phone link. Ops console shows startup-native agent-trace placeholder lines. Any visible h2/eyebrow "SMEs" strings are neutralized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML → browser | Static file served from Netlify CDN; no user input in this plan |
| External CDN | Google Fonts stylesheet link added to <head> — font files from fonts.gstatic.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | index.html footer Gmail address | accept | Phase 4 replaces with branded email; Gmail in footer is a known interim state documented in STATE.md blockers |
| T-02-02 | Tampering | Google Fonts CDN link | accept | Fonts.googleapis.com is a trusted Google CDN; font loading failure degrades gracefully to system font stack |
</threat_model>

<verification>
After both tasks complete, open index.html in a browser and confirm:
1. Page title in browser tab reads "AgentOps Studio | AI Engineering for Founders & Growing Teams"
2. Header shows floating pill with "Services / Work / Process / About / Contact" labels and "Book a call" button
3. Hero H1 reads "Build AI products that scale."
4. No phone number visible anywhere on the page
5. No "Book an automation audit" text visible
6. No packages / tier pricing section visible

Grep gates (run from project root):
```bash
grep -c 'AI Engineering for Founders' index.html          # must be 1
grep -c 'header-positioner' index.html                    # must be 1
grep -c 'class="nav-link"' index.html                     # must be 5
grep -c 'btn-book-call' index.html                        # must be 1
grep -c 'Build AI products that scale.' index.html        # must be 1
grep -ic 'automation audit\|SMEs\|packages-section' index.html  # must be 0
grep -c 'href="tel:' index.html                           # must be 0
```
</verification>

<success_criteria>
- index.html &lt;head&gt; has: preconnect to fonts.googleapis.com, preconnect to fonts.gstatic.com with crossorigin, Inter variable font stylesheet link, all before styles.css
- &lt;title&gt; is "AgentOps Studio | AI Engineering for Founders & Growing Teams"
- &lt;meta name="description"&gt; is "End-to-end AI product builds, agent orchestration, and DevOps for startups and growing businesses."
- Header is &lt;div class="header-positioner" data-header&gt; wrapping &lt;header class="site-header"&gt; — data-header on outer wrapper only
- Nav has five anchors with class="nav-link" and labels: Services / Work / Process / About / Contact (hrefs unchanged)
- &lt;a class="btn-primary btn-book-call" href="#contact"&gt;Book a call&lt;/a&gt; present after the nav
- Hero H1: "Build AI products that scale."
- Hero lede: "End-to-end AI engineering for founders who need it done right."
- Primary hero CTA: "Book a discovery call"
- Secondary hero CTA: "View work"
- .hero-contact-links div: removed
- .hero-metrics div: removed
- packages-section: removed
- Ops console: three startup-native flow rows (Orchestrator, Data Agent, Monitoring Agent)
- Contact section: no .direct-contact div, no phone/WhatsApp input field
- Footer: no phone link
- Zero occurrences of: "automation audit", "SMEs", "small and medium businesses", "We build", href="tel:"
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` using the summary template at `@/Users/manikmalhotra/.claude/get-shit-done/templates/summary.md`.
</output>
