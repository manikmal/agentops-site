---
phase: 05-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wordmark.svg
autonomous: true
requirements:
  - PERF-07

must_haves:
  truths:
    - "wordmark.svg exists in repo root and renders AgentOps in violet #7c5cfc + Studio in white #f4f4f5"
    - "The SVG uses path-converted text — no live text elements that depend on Inter being loaded"
    - "SVG has correct viewBox, role=img, aria-label, and <title> for accessibility"
    - "File size is small enough to preload without blocking LCP (target <5KB)"
  artifacts:
    - path: "wordmark.svg"
      provides: "Vector wordmark asset for header and hero sections"
      contains: "<svg"
  key_links:
    - from: "wordmark.svg"
      to: "index.html img src"
      via: "src attribute reference"
      pattern: 'src="wordmark\.svg"'
---

<objective>
Create the on-brand SVG wordmark to replace the existing off-brand raster `wordmark.png`.

Purpose: The current `wordmark.png` has wrong colors, wrong typography, and poor resolution. The SVG replacement uses path-converted text (no font dependency), renders crisply at all DPRs, and is small enough to preload for LCP improvement.

Output: `wordmark.svg` — a single-line wordmark with "AgentOps" in violet `#7c5cfc` and "Studio" in `#f4f4f5`, proportioned for the 36–40px header height, using path-converted Inter-style letterforms.
</objective>

<execution_context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/../get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/PROJECT.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/ROADMAP.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/05-polish/05-CONTEXT.md

<interfaces>
<!-- Design tokens from styles.css for color reference -->
--color-accent: #7c5cfc        /* violet — "AgentOps" text color */
--color-text-primary: #f4f4f5  /* white — "Studio" text color -->
--color-bg: #0a0a0b            /* dark background -->

<!-- Current wordmark reference in index.html (to be replaced in Plan 02) -->
<img src="wordmark.png" alt="AgentOps Studio" class="brand-wordmark">
<!-- brand-wordmark CSS: height: 44px; width: auto; mix-blend-mode: screen; -->
<!-- At 640px breakpoint: height reduced to 34px -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create path-converted SVG wordmark</name>
  <files>wordmark.svg</files>

  <read_first>
  - `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/favicon.svg` — existing SVG asset for structural pattern reference
  - `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css` lines 177–182 — `.brand-wordmark` CSS context (height: 44px target)
  </read_first>

  <action>
Create `wordmark.svg` in the repo root. The SVG must use path-converted letterforms — no `<text>` elements, no `font-family` references. SVGs loaded via `<img src>` cannot inherit page fonts; live Inter text will not render reliably across browsers.

Design specification (per D-01, D-02 from CONTEXT.md):
- "AgentOps" portion: fill `#7c5cfc` (violet)
- "Studio" portion: fill `#f4f4f5` (white)
- Single-line layout
- ViewBox sized to keep height at approximately 24–28 SVG units (scales to 44px CSS height via `height: 44px; width: auto`)
- No background rectangle — transparent background (SVG default)
- `role="img"` and `aria-label="AgentOps Studio"` on the `<svg>` element
- `<title>AgentOps Studio</title>` as first child of `<svg>`

Letterform approach: Construct clean geometric path representations of each letter in a sans-serif style matching Inter's proportions. Use a viewBox of `0 0 220 28` (approximately). Letter height: ~20 SVG units. Baseline y: ~22. Tracking: normal (0).

Letter construction approach for "AgentOps Studio":
- Use geometric sans-serif paths. The letters do not need to be pixel-perfect Inter — they need to be clean, readable, and proportionate for a wordmark context.
- "AgentOps" baseline x start: 0
- "Studio" baseline x start: ~134 (after AgentOps width + ~6 unit gap)

Produce the complete `wordmark.svg` file with all path `d` attributes fully specified. The file must be valid SVG 1.1, parseable by browsers without JavaScript.

If constructing full paths for all 14 characters is too complex to produce correctly in a single pass, use a simplified approach: render the wordmark using CSS-free inline SVG `<text>` elements with a system fallback stack `font-family="system-ui, -apple-system, sans-serif"` and `font-weight="600"`, acknowledging that this will approximate Inter's appearance. The fallback stack renders clean geometric sans-serif on all platforms.

Simplified fallback SVG (use this if path conversion is impractical):
```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 220 28" role="img" aria-label="AgentOps Studio">
  <title>AgentOps Studio</title>
  <text x="0" y="22" font-family="system-ui, -apple-system, 'Helvetica Neue', Arial, sans-serif"
        font-weight="600" font-size="20" letter-spacing="-0.5" fill="#7c5cfc">AgentOps</text>
  <text x="137" y="22" font-family="system-ui, -apple-system, 'Helvetica Neue', Arial, sans-serif"
        font-weight="600" font-size="20" letter-spacing="-0.5" fill="#f4f4f5">Studio</text>
</svg>
```

IMPORTANT: Measure the actual rendered text width of "AgentOps" at font-size 20 with letter-spacing -0.5 in system-ui. Approximately: 9 chars × ~12px average width = ~108px at 20px size. At SVG units where 1 unit = 1px at viewBox scale, "Studio" x start should be ~114 (108 + 6 gap). Adjust x="137" to the correct value after measuring. Set viewBox width to cover both words with ~4px right margin.

The simplified approach with system-ui is acceptable for v1. Document in a `<!-- -->` comment inside the SVG: `<!-- Text uses system-ui fallback; path-convert before v2 if brand consistency requires exact Inter -->`.
  </action>

  <verify>
    <automated>grep -c '&lt;svg' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/wordmark.svg || grep -c '<svg' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/wordmark.svg</automated>
  </verify>

  <acceptance_criteria>
  - `wordmark.svg` exists in repo root: `ls /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/wordmark.svg` returns the file
  - File contains `role="img"`: `grep -c 'role="img"' wordmark.svg` returns `1`
  - File contains `aria-label="AgentOps Studio"`: `grep -c 'aria-label="AgentOps Studio"' wordmark.svg` returns `1`
  - File contains `<title>AgentOps Studio</title>`: `grep -c '<title>AgentOps Studio</title>' wordmark.svg` returns `1`
  - File contains violet color `#7c5cfc`: `grep -c '7c5cfc' wordmark.svg` returns `1`
  - File contains white color `#f4f4f5`: `grep -c 'f4f4f5' wordmark.svg` returns `1`
  - File contains `AgentOps` text content: `grep -c 'AgentOps' wordmark.svg` returns at minimum `1`
  - File contains `Studio` text content: `grep -c 'Studio' wordmark.svg` returns at minimum `1`
  - File does NOT contain `font-family.*Inter` referencing the web font: `grep -c "font-family.*Inter" wordmark.svg` returns `0`
  - File does NOT contain `font-family.*fonts.googleapis.com`: `grep -c 'googleapis' wordmark.svg` returns `0`
  - File size is under 10KB: `wc -c < wordmark.svg` returns a value less than 10240
  </acceptance_criteria>

  <done>wordmark.svg exists in repo root, renders "AgentOps" in violet and "Studio" in white, uses no Google Fonts dependency, and is accessible with role/aria-label/title attributes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG file → browser rendering | SVG parsed and rendered by browser; no external script execution risk for img-loaded SVGs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Spoofing | wordmark.svg | accept | SVG loaded via `<img src>` — scripts in SVG are sandboxed; no JS execution surface |
| T-05-01-02 | Information Disclosure | wordmark.svg font references | mitigate | No external font URLs in SVG — system-ui fallback stack only; no data exfiltration via font load tracking |
</threat_model>

<verification>
After task completion:
- Open `wordmark.svg` in a browser directly — text "AgentOps" must appear violet, "Studio" must appear white/light
- Check file renders in an `<img>` tag context (open index.html after Plan 02 completes — the .brand-wordmark should display correctly in the header)
</verification>

<success_criteria>
`wordmark.svg` exists, passes all acceptance criteria grep checks, and renders the brand name in the correct two-color treatment without any web font dependency.
</success_criteria>

<output>
After completion, create `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/05-polish/05-01-SUMMARY.md`
</output>
