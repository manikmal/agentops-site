---
phase: 05-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - script.js
autonomous: true
requirements:
  - PERF-03
  - PERF-04
  - PERF-05

must_haves:
  truths:
    - "Canvas requestIdleCallback init block is guarded by !window.matchMedia('(prefers-reduced-motion: reduce)').matches"
    - "visibilitychange resume handler is guarded by the same reduced-motion check"
    - "All below-fold img elements in index.html have loading='lazy' and explicit width/height attributes"
  artifacts:
    - path: "script.js"
      provides: "Canvas init with prefers-reduced-motion guard at the block level"
      contains: "prefers-reduced-motion"
    - path: "index.html"
      provides: "Below-fold images with lazy loading and explicit dimensions"
      contains: 'loading="lazy"'
  key_links:
    - from: "script.js canvas init block"
      to: "window.matchMedia('(prefers-reduced-motion: reduce)').matches"
      via: "!matchMedia guard in if condition at lines 302–314"
      pattern: "matchMedia.*prefers-reduced-motion.*canvas"
    - from: "script.js visibilitychange handler"
      to: "window.matchMedia('(prefers-reduced-motion: reduce)').matches"
      via: "!matchMedia guard in else-if condition at line 326"
      pattern: "matchMedia.*prefers-reduced-motion.*visibilitychange"
---

<objective>
Fix the missing `prefers-reduced-motion` guard on the canvas animation init, and audit below-fold images for lazy loading attributes.

Purpose: The canvas `requestIdleCallback` init block at `script.js` lines 302–314 unconditionally starts the rAF loop — it has NO reduced-motion guard. This contradicts CONTEXT.md D-09 (which incorrectly states the guard is in place). The PATTERNS.md key finding confirms the bug. This plan fixes it. The image audit satisfies PERF-05 (lazy loading).

Output: Two targeted edits to `script.js` (canvas init guard + visibilitychange guard) and any missing `loading="lazy"` + `width`/`height` attributes on below-fold `<img>` elements in `index.html`.
</objective>

<execution_context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/../get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/PROJECT.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/ROADMAP.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/05-polish/05-CONTEXT.md

<interfaces>
<!-- script.js canvas init block — current state (lines 301–314): -->
<!--
// Deferred init via requestIdleCallback per D-17; setTimeout fallback for Safari
if (canvas && context) {
  const initCanvas = () => {
    resizeCanvas();
    cancelAnimationFrame(animationFrame);
    drawNetwork();
    window.addEventListener("resize", debounce(resizeCanvas, 180));
  };
  if ("requestIdleCallback" in window) {
    requestIdleCallback(initCanvas, { timeout: 2000 });
  } else {
    setTimeout(initCanvas, 200);
  }
}
-->

<!-- script.js visibilitychange handler — current state (lines 321–330): -->
<!--
document.addEventListener("visibilitychange", () => {
  if (document.hidden) {
    cancelAnimationFrame(animationFrame);
    clearTimeout(traceTimeout);
    clearInterval(elapsedInterval);
  } else if (canvas && context) {
    cancelAnimationFrame(animationFrame);
    drawNetwork();
  }
});
-->

<!-- Analog matchMedia pattern already used at line 8: -->
<!-- if (marqueeTrack && !window.matchMedia("(prefers-reduced-motion: reduce)").matches) { -->

<!-- Analog matchMedia pattern already used at line 116 (inside counter animation): -->
<!-- if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return; -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prefers-reduced-motion guard to canvas init and visibilitychange handler</name>
  <files>script.js</files>

  <read_first>
  - `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/script.js` lines 295–340 — canvas init block and visibilitychange handler (full context of both edits)
  - `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/script.js` lines 1–20 — marquee guard pattern at line 8 (analog for exact syntax)
  </read_first>

  <action>
**Edit 1 — Canvas init block (lines 301–314)**

Find the current block:
```js
// Deferred init via requestIdleCallback per D-17; setTimeout fallback for Safari
if (canvas && context) {
  const initCanvas = () => {
    resizeCanvas();
    cancelAnimationFrame(animationFrame);
    drawNetwork();
    window.addEventListener("resize", debounce(resizeCanvas, 180));
  };
  if ("requestIdleCallback" in window) {
    requestIdleCallback(initCanvas, { timeout: 2000 });
  } else {
    setTimeout(initCanvas, 200);
  }
}
```

Replace with (add `!window.matchMedia('(prefers-reduced-motion: reduce)').matches` to the `if` condition):
```js
// Deferred init via requestIdleCallback per D-17; setTimeout fallback for Safari
if (canvas && context && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
  const initCanvas = () => {
    resizeCanvas();
    cancelAnimationFrame(animationFrame);
    drawNetwork();
    window.addEventListener("resize", debounce(resizeCanvas, 180));
  };
  if ("requestIdleCallback" in window) {
    requestIdleCallback(initCanvas, { timeout: 2000 });
  } else {
    setTimeout(initCanvas, 200);
  }
}
```

The only change is adding `&& !window.matchMedia('(prefers-reduced-motion: reduce)').matches` to the outer `if` condition. All inner logic is unchanged.

**Edit 2 — visibilitychange resume handler (lines 326–329)**

Find the `else if` clause inside the visibilitychange listener:
```js
  } else if (canvas && context) {
    cancelAnimationFrame(animationFrame);
    drawNetwork();
  }
```

Replace with:
```js
  } else if (canvas && context && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    cancelAnimationFrame(animationFrame);
    drawNetwork();
  }
```

Again, only the `else if` condition changes — add `&& !window.matchMedia('(prefers-reduced-motion: reduce)').matches`. The `if (document.hidden)` branch (which cancels the rAF) must remain unchanged — it is correct to cancel the animation regardless of reduced-motion preference.

**Behavior after fix:**
- When `prefers-reduced-motion: reduce` is set: canvas init is skipped entirely, the `<canvas>` element remains in the DOM but blank. The visibilitychange handler will also skip the `drawNetwork()` resume call.
- When `prefers-reduced-motion: reduce` is NOT set: behavior identical to current — canvas inits via requestIdleCallback, animates normally.
  </action>

  <verify>
    <automated>grep -c 'prefers-reduced-motion.*canvas\|canvas.*prefers-reduced-motion' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/script.js</automated>
  </verify>

  <acceptance_criteria>
  - Canvas init if-condition includes reduced-motion check: `grep -c "canvas && context && !window.matchMedia" script.js` returns `1`
  - visibilitychange else-if includes reduced-motion check: the string `else if (canvas && context && !window.matchMedia` appears in script.js: `grep -c "else if (canvas && context && !window.matchMedia" script.js` returns `1`
  - The `if (document.hidden)` branch is unchanged: `grep -c 'document.hidden' script.js` returns `1`
  - The `drawNetwork` call inside visibilitychange is still present (just gated): `grep -c 'drawNetwork' script.js` returns at least `2` (one in initCanvas, one in visibilitychange)
  - requestIdleCallback is still used: `grep -c 'requestIdleCallback' script.js` returns at least `1`
  - setTimeout fallback still present: `grep -c 'setTimeout(initCanvas' script.js` returns `1`
  - The canvas `<canvas>` data attribute selector is not changed: `grep -c 'data-agent-canvas\|agent-canvas' script.js` returns at least `1`
  </acceptance_criteria>

  <done>The canvas init block and visibilitychange resume handler both check !window.matchMedia('(prefers-reduced-motion: reduce)').matches before starting the rAF animation loop.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and add lazy loading attributes to below-fold images</name>
  <files>index.html</files>

  <read_first>
  - `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html` — full file scan for all `<img>` elements to identify which are above-fold (eager) vs below-fold (lazy)
  </read_first>

  <action>
Audit every `<img>` tag in `index.html` and apply `loading="lazy"` to all below-fold images. Also ensure all `<img>` elements have explicit `width` and `height` attributes to prevent Cumulative Layout Shift (CLS).

**Above-fold images (MUST stay eager — do NOT add loading="lazy"):**
- `.brand-icon` (favicon.svg in header) — header is immediately visible
- `.brand-wordmark` (wordmark.svg in header) — LCP candidate, must not be lazy
- `.hero-wordmark` (wordmark.svg in hero) — above the fold

**Below-fold images (add loading="lazy" if not already present):**
Any `<img>` not in the `.site-header` or `.hero` section. Common candidates in this codebase:
- Logo strip `<img>` elements in the `.logo-strip` / `.marquee-track` section
- Portfolio card images (if any `<img>` tags exist in `.portfolio-grid`)
- About section image (if any `<img>` tag exists in `.about-grid`)
- Tech strip logos (if raster images, not inline SVG)
- Any other `<img>` below the hero section

**For each below-fold `<img>` found:**
1. Add `loading="lazy"` attribute if not present
2. Ensure `width` and `height` attributes are present with numeric pixel values matching the image's natural dimensions or intended display size. If the image's natural dimensions are not known, use the CSS-rendered dimensions as the attribute values (prevents CLS).

**For above-fold `<img>` elements without width/height:**
Add `width` and `height` even to eager images if they are missing — this prevents CLS regardless of load timing.

**Canonical scan approach:**
Run `grep -n '<img' index.html` to get a list of all img tags. For each one:
- Determine if above-fold or below-fold by its parent section
- Apply lazy loading to below-fold
- Verify width/height present on all

If the tech strip uses inline SVG `<svg>` elements (not `<img>`), they do not need lazy loading. Verify by checking whether the tech strip pills use SVG `<use>` or `<img>` or inline SVG.

Note: The `.brand-icon` already has `width="36" height="36"` per the current index.html (confirmed in head section read). Preserve this.
  </action>

  <verify>
    <automated>grep -n 'loading="lazy"' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html</automated>
  </verify>

  <acceptance_criteria>
  - All below-fold `<img>` elements have `loading="lazy"`: After task, verify `grep -n '<img' index.html` output — every img tag outside of `.site-header` and `.hero` sections must contain `loading="lazy"`
  - No above-fold image has `loading="lazy"` added: The `.brand-icon`, `.brand-wordmark`, and `.hero-wordmark` img tags must NOT contain `loading="lazy"`
  - Hero wordmark does NOT have lazy: `grep 'hero-wordmark' index.html | grep -c 'loading="lazy"'` returns `0`
  - Header wordmark does NOT have lazy: `grep 'brand-wordmark' index.html | grep -c 'loading="lazy"'` returns `0`
  - If any below-fold images were found, at least one has lazy: `grep -c 'loading="lazy"' index.html` returns at least `1` (or `0` if no raster below-fold images exist — document this finding in SUMMARY)
  - `brand-icon` retains `width="36" height="36"`: `grep -c 'width="36" height="36"' index.html` returns `1`
  </acceptance_criteria>

  <done>All below-fold img elements have loading="lazy" and explicit width/height. Above-fold images (brand-icon, brand-wordmark, hero-wordmark) remain eager-loaded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS canvas → browser rendering thread | Canvas rAF loop runs on the main thread; reduced-motion guard protects users with vestibular disorders |
| img loading="lazy" → browser preload scanner | lazy attribute instructs browser not to fetch until near-viewport; no security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Denial of Service | canvas rAF loop | mitigate | prefers-reduced-motion guard prevents canvas from running on devices where animation causes problems (vestibular disorders, low-power mode) |
| T-05-04-02 | Information Disclosure | loading="lazy" image requests | accept | Lazy-loaded images reveal scroll depth to server logs — no PII involved; standard web behavior |
| T-05-04-03 | Tampering | window.matchMedia spoofing | accept | matchMedia is read-only browser API; no user-controlled input path to this check |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "canvas && context && !window.matchMedia" script.js` returns `1`
2. `grep -c "else if (canvas && context && !window.matchMedia" script.js` returns `1`
3. In Chrome DevTools → Rendering → "Emulate CSS media feature prefers-reduced-motion: reduce" — canvas must remain blank/empty
4. Without the emulation active, canvas must animate normally on page load
5. `grep -n 'loading="lazy"' index.html` lists any below-fold images correctly tagged
</verification>

<success_criteria>
script.js canvas init and visibilitychange handler both check prefers-reduced-motion before starting the rAF loop. All below-fold images in index.html have loading="lazy" and explicit dimensions.
</success_criteria>

<output>
After completion, create `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/05-polish/05-04-SUMMARY.md`
</output>
