---
phase: 05-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - PERF-06
  - PERF-07

must_haves:
  truths:
    - "Social meta tags are present — og:title, og:description, og:image, og:type, og:url, twitter:card, twitter:title, twitter:description, twitter:image"
    - "og:image and twitter:image point to the absolute URL https://agentopsstudio.com/og-image.png"
    - "wordmark.svg is preloaded with <link rel=preload as=image>"
    - "Google Fonts Inter stylesheet is preloaded with <link rel=preload as=style>"
    - "Both wordmark.png references in index.html are updated to wordmark.svg"
    - "The Book a call button is repositioned in the header DOM — between .brand and .nav-toggle"
  artifacts:
    - path: "index.html"
      provides: "Updated HTML with og: meta block, preload hints, wordmark.svg references, and mobile CTA DOM position"
      contains: 'og:title'
  key_links:
    - from: "index.html og:image"
      to: "og-image.png"
      via: "absolute URL https://agentopsstudio.com/og-image.png"
      pattern: 'og-image\.png'
    - from: "index.html .brand-wordmark"
      to: "wordmark.svg"
      via: "img src attribute"
      pattern: 'wordmark\.svg'
    - from: ".btn-book-call"
      to: ".nav-toggle"
      via: "DOM position — btn-book-call immediately precedes nav-toggle in .site-header"
      pattern: 'btn-book-call'
---

<objective>
Update `index.html` to add the full social meta block, LCP preload hints, swap both `wordmark.png` references to `wordmark.svg`, and reposition the "Book a call" button in the header DOM for always-visible mobile display.

Purpose: Satisfies PERF-06 (og: meta for social sharing) and PERF-07 (LCP hints for Lighthouse). The mobile CTA DOM move (D-06, D-07) is required for Plan 03's CSS to correctly pin the button outside the hamburger menu.

Output: Updated `index.html` with og: meta block in `<head>`, two preload hints, wordmark.svg in both img tags, and the mobile CTA correctly positioned in the header flex row.
</objective>

<execution_context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/../get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/PROJECT.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/ROADMAP.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/05-polish/05-CONTEXT.md

<interfaces>
<!-- Current index.html <head> (lines 1–21): -->
<!--
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="End-to-end AI product builds, agent orchestration, and DevOps for startups and growing businesses.">
  <title>AgentOps Studio | AI Engineering for Founders & Growing Teams</title>
  <script>document.documentElement.classList.add("js");</script>
  <noscript>...</noscript>
  <link rel="icon" type="image/svg+xml" href="favicon.svg">
  <link rel="icon" type="image/png" href="logo.png">
  <link rel="apple-touch-icon" href="logo.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
-->

<!-- Current header structure (lines 24–40): -->
<!--
  <div class="header-positioner" data-header>
    <header class="site-header">
      <a class="brand" href="#top" aria-label="AgentOps Studio home">
        <img src="favicon.svg" alt="" class="brand-icon" aria-hidden="true" width="36" height="36">
        <img src="wordmark.png" alt="AgentOps Studio" class="brand-wordmark">
      </a>
      <button class="nav-toggle" data-nav-toggle aria-expanded="false" aria-controls="site-nav">Menu</button>
      <nav class="site-nav" id="site-nav" data-nav>
        <a class="nav-link" href="#services">Services</a>
        <a class="nav-link" href="#portfolio">Work</a>
        <a class="nav-link" href="#process">Process</a>
        <a class="nav-link" href="#about">About</a>
        <a class="nav-link" href="#contact">Contact</a>
      </nav>
      <a class="btn-primary btn-book-call" href="#contact">Book a call</a>
    </header>
  </div>
-->

<!-- Hero wordmark reference (line 47): -->
<!-- <img src="wordmark.png" alt="AgentOps Studio" class="hero-wordmark"> -->

<!-- .site-header CSS: display: flex; align-items: center; justify-content: space-between -->
<!-- .btn-primary: existing violet CTA button class — reused as-is, no new classes needed -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add og: meta block and preload hints to &lt;head&gt;</name>
  <files>index.html</files>

  <read_first>
  - `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html` lines 1–22 — current `<head>` to see exact insertion points
  </read_first>

  <action>
Make two insertions in `index.html`'s `<head>` section:

**Insertion 1 — og: meta block**
Insert immediately after the `<meta name="description" ...>` line (currently line 6). Add these exact tags:

```html
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://agentopsstudio.com/">
    <meta property="og:title" content="AgentOps Studio | AI Engineering for Founders &amp; Growing Teams">
    <meta property="og:description" content="End-to-end AI product builds, agent orchestration, and DevOps for startups and growing businesses.">
    <meta property="og:image" content="https://agentopsstudio.com/og-image.png">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="AgentOps Studio | AI Engineering for Founders &amp; Growing Teams">
    <meta name="twitter:description" content="End-to-end AI product builds, agent orchestration, and DevOps for startups and growing businesses.">
    <meta name="twitter:image" content="https://agentopsstudio.com/og-image.png">
```

**Insertion 2 — preload hints**
Insert immediately after the `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` line (the second preconnect). Add these exact tags:

```html
    <link rel="preload" as="image" href="wordmark.svg">
    <link rel="preload" href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap" as="style">
```

Per D-11: the two `<link rel="preconnect">` tags already present must be preserved unchanged.
  </action>

  <verify>
    <automated>grep -c 'og:title' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html && grep -c 'preload.*wordmark\.svg' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html</automated>
  </verify>

  <acceptance_criteria>
  - `og:type` present: `grep -c 'property="og:type"' index.html` returns `1`
  - `og:url` present: `grep -c 'property="og:url"' index.html` returns `1`
  - `og:title` present: `grep -c 'property="og:title"' index.html` returns `1`
  - `og:description` present: `grep -c 'property="og:description"' index.html` returns `1`
  - `og:image` points to correct absolute URL: `grep -c 'https://agentopsstudio.com/og-image.png' index.html` returns `2` (og:image + twitter:image)
  - `twitter:card` is `summary_large_image`: `grep -c 'content="summary_large_image"' index.html` returns `1`
  - `twitter:title` present: `grep -c 'name="twitter:title"' index.html` returns `1`
  - `twitter:description` present: `grep -c 'name="twitter:description"' index.html` returns `1`
  - `twitter:image` present: `grep -c 'name="twitter:image"' index.html` returns `1`
  - wordmark.svg preload present: `grep -c 'rel="preload".*wordmark\.svg\|wordmark\.svg.*rel="preload"' index.html` returns `1`
  - Inter font preload present: `grep -c 'rel="preload".*fonts\.googleapis' index.html` returns `1`
  - Both preconnect links still present: `grep -c 'rel="preconnect"' index.html` returns `2`
  </acceptance_criteria>

  <done>All 9 og:/twitter: meta tags are present in the head, both preload hints are present, and preconnect links are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Swap wordmark.png to wordmark.svg and reposition mobile CTA</name>
  <files>index.html</files>

  <read_first>
  - `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html` lines 24–60 — current header structure and hero wordmark reference
  </read_first>

  <action>
Make three targeted changes to `index.html`:

**Change 1 — header wordmark src**
Find: `<img src="wordmark.png" alt="AgentOps Studio" class="brand-wordmark">`
Replace with: `<img src="wordmark.svg" alt="AgentOps Studio" class="brand-wordmark">`

**Change 2 — hero wordmark src**
Find: `<img src="wordmark.png" alt="AgentOps Studio" class="hero-wordmark">`
Replace with: `<img src="wordmark.svg" alt="AgentOps Studio" class="hero-wordmark">`

**Change 3 — reposition .btn-book-call in header**
The current header DOM order is: `.brand` → `.nav-toggle` → `.site-nav` → `.btn-book-call`
The required order is: `.brand` → `.btn-book-call` → `.nav-toggle` → `.site-nav`

Current structure to replace:
```html
      <a class="brand" href="#top" aria-label="AgentOps Studio home">
        <img src="favicon.svg" alt="" class="brand-icon" aria-hidden="true" width="36" height="36">
        <img src="wordmark.svg" alt="AgentOps Studio" class="brand-wordmark">
      </a>
      <button class="nav-toggle" data-nav-toggle aria-expanded="false" aria-controls="site-nav">Menu</button>
      <nav class="site-nav" id="site-nav" data-nav>
        <a class="nav-link" href="#services">Services</a>
        <a class="nav-link" href="#portfolio">Work</a>
        <a class="nav-link" href="#process">Process</a>
        <a class="nav-link" href="#about">About</a>
        <a class="nav-link" href="#contact">Contact</a>
      </nav>
      <a class="btn-primary btn-book-call" href="#contact">Book a call</a>
```

Replace with:
```html
      <a class="brand" href="#top" aria-label="AgentOps Studio home">
        <img src="favicon.svg" alt="" class="brand-icon" aria-hidden="true" width="36" height="36">
        <img src="wordmark.svg" alt="AgentOps Studio" class="brand-wordmark">
      </a>
      <a class="btn-primary btn-book-call" href="#contact">Book a call</a>
      <button class="nav-toggle" data-nav-toggle aria-expanded="false" aria-controls="site-nav">Menu</button>
      <nav class="site-nav" id="site-nav" data-nav>
        <a class="nav-link" href="#services">Services</a>
        <a class="nav-link" href="#portfolio">Work</a>
        <a class="nav-link" href="#process">Process</a>
        <a class="nav-link" href="#about">About</a>
        <a class="nav-link" href="#contact">Contact</a>
      </nav>
```

Note: The `href="#contact"` on the Calendly link — per STATE.md the Calendly URL is `https://calendly.com/manikmalhotra6/30min`. The `.btn-book-call` currently uses `href="#contact"` — keep this as-is; the Calendly URL wiring is pre-existing work from Phase 4. Do not change the href value.

Note: Apply Change 1 (wordmark.png → wordmark.svg in brand) BEFORE the DOM reorder, or coordinate so the final state of the `.brand` element has `wordmark.svg`. If you do the reorder first, ensure the wordmark src is `wordmark.svg` in the reordered result.
  </action>

  <verify>
    <automated>grep -c 'wordmark\.png' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html</automated>
  </verify>

  <acceptance_criteria>
  - No remaining `wordmark.png` references: `grep -c 'wordmark\.png' index.html` returns `0`
  - Both wordmark.svg references present: `grep -c 'wordmark\.svg' index.html` returns `2` (header .brand-wordmark + hero .hero-wordmark)
  - `.btn-book-call` appears before `.nav-toggle` in file: verify with `grep -n 'btn-book-call\|nav-toggle' index.html` — the `btn-book-call` line number must be LOWER than the `nav-toggle` line number
  - `.btn-book-call` appears before `<nav` in file: `grep -n 'btn-book-call\|<nav ' index.html` — btn-book-call line number must be LOWER than nav line number
  - `.site-header` still contains exactly one `.btn-book-call` element: `grep -c 'btn-book-call' index.html` returns `1`
  - `data-nav-toggle` attribute preserved on button: `grep -c 'data-nav-toggle' index.html` returns `1`
  - All 5 nav links still present: `grep -c 'class="nav-link"' index.html` returns `5`
  </acceptance_criteria>

  <done>Both wordmark.png references are replaced with wordmark.svg, and the .btn-book-call element is between .brand and .nav-toggle in the header DOM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.html → browser | HTML parsed by browser; og: meta consumed by crawlers and social platform scrapers |
| og:image URL → CDN | Absolute URL in og:image must resolve to a valid image; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Spoofing | og:image URL | accept | URL is hardcoded to agentopsstudio.com — no dynamic input, no injection surface |
| T-05-02-02 | Information Disclosure | og:description | accept | Description is public marketing copy — no PII or internal information |
| T-05-02-03 | Tampering | wordmark.svg src | accept | Static asset URL — no user-controlled input; server must serve the file |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c 'wordmark\.png' index.html` must return `0`
2. `grep -c 'og:title' index.html` must return `1`
3. `grep -c 'btn-book-call' index.html` must return `1`
4. Open `index.html` in a browser — the header should show the wordmark, the "Book a call" button, the hamburger button (on mobile), and the nav
5. Validate the head with an og:tag validator (opengraph.xyz or similar) after deploying
</verification>

<success_criteria>
index.html contains the complete og: meta block with absolute URLs, both wordmark.svg preloads, no remaining wordmark.png references, and the Book a call button is positioned between the brand and hamburger in the DOM.
</success_criteria>

<output>
After completion, create `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/05-polish/05-02-SUMMARY.md`
</output>
