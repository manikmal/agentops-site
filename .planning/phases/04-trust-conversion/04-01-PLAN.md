---
phase: 04-trust-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - styles.css
autonomous: true
requirements:
  - PROC-01
  - PROC-02
  - PROC-03

must_haves:
  truths:
    - "Process section heading reads 'From first conversation to shipped product.' with eyebrow 'How we work'"
    - "Process section has exactly 5 timeline-step articles with step numbers 01–05"
    - "Step names are Discovery, Scope, Build, Ship, Iterate in that order"
    - "Each step description contains async-first language drawn from the Copywriting Contract"
    - "Process section has id='process' so the nav Process link resolves correctly"
    - "Timeline desktop grid is repeat(5, minmax(0, 1fr)) — not repeat(4, ...)"
  artifacts:
    - path: "index.html"
      provides: "5-step process section with id=process, correct copy, all 5 timeline-step articles"
      contains: "id=\"process\""
    - path: "styles.css"
      provides: "5-column timeline grid rule"
      contains: "repeat(5, minmax(0, 1fr))"
  key_links:
    - from: "nav 'Process' link"
      to: "#process"
      via: "href attribute on nav-link"
      pattern: "href=\"#process\""
    - from: ".timeline CSS"
      to: "5 article children"
      via: "grid-template-columns: repeat(5, minmax(0, 1fr))"
      pattern: "repeat\\(5"
---

<objective>
Rewrite the process section: update the heading, add `id="process"` to the section element, replace 4 SME-era timeline steps with 5 founder-POV steps (Discovery, Scope, Build, Ship, Iterate) using exact copy from the Copywriting Contract, and update the CSS grid from 4 to 5 columns. Also fix the nav "Process" href from `#contact` to `#process`.

Purpose: Delivers PROC-01 (5 founder-POV steps), PROC-02 (async-first language), PROC-03 (horizontal timeline). Establishes the "how I work" section before the About section is inserted in Plan 02.

Output: Updated index.html (process section + nav href), updated styles.css (grid column count).
</objective>

<execution_context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/04-trust-conversion/04-UI-SPEC.md
</execution_context>

<context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/ROADMAP.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/STATE.md

<interfaces>
<!-- Current state of relevant HTML and CSS extracted for executor reference. -->

index.html line 33–36 (nav links — Process and About both incorrectly point to #contact):
```html
<a class="nav-link" href="#contact">Process</a>
<a class="nav-link" href="#contact">About</a>
```

index.html lines 243–270 (process section — no id attribute, 4 steps):
```html
<section class="section process-section">
  <div class="section-heading reveal">
    <p class="eyebrow">How I work</p>
    <h2>From messy workflow to managed system.</h2>
  </div>
  <div class="timeline">
    <article class="timeline-step reveal">
      <span>01</span>
      <h3>Audit</h3>
      <p>Map the manual work, tool gaps, recurring delays...</p>
    </article>
    <!-- 3 more steps: Design (02), Build (03), Run (04) -->
  </div>
</section>
```

styles.css line 779 (timeline grid — 4 columns today):
```css
.timeline {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 16px;
}
```

styles.css lines 903–907 (940px breakpoint — timeline already collapses to 2fr):
```css
.portfolio-grid,
.service-grid,
.timeline {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}
```

styles.css lines 932–936 (640px breakpoint — timeline collapses to 1fr):
```css
.portfolio-grid,
.service-grid,
.timeline {
  grid-template-columns: 1fr;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix nav href and update process section heading + id</name>
  <read_first>
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html (read in full before editing — understand current nav and process section structure)
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/04-trust-conversion/04-UI-SPEC.md (Copywriting Contract — Process Section table)
  </read_first>
  <files>index.html</files>
  <action>
Make three changes to index.html:

1. Fix nav "Process" href (line 34):
   Change: `<a class="nav-link" href="#contact">Process</a>`
   To: `<a class="nav-link" href="#process">Process</a>`

   Leave the "About" nav link as-is for now (Plan 02 will set it to `#about` when the About section is inserted).

2. Add `id="process"` to the process section element (line 243):
   Change: `<section class="section process-section">`
   To: `<section class="section process-section" id="process">`

3. Replace the section-heading block inside the process section:
   Change:
   ```html
   <div class="section-heading reveal">
     <p class="eyebrow">How I work</p>
     <h2>From messy workflow to managed system.</h2>
   </div>
   ```
   To:
   ```html
   <div class="section-heading reveal">
     <p class="eyebrow">How we work</p>
     <h2>From first conversation to shipped product.</h2>
   </div>
   ```

Do not touch the timeline steps yet — Task 2 handles those.
  </action>
  <verify>
    <automated>grep -n 'href="#process"' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html && grep -n 'id="process"' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html && grep -n 'From first conversation to shipped product' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html && grep -n 'How we work' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html</automated>
  </verify>
  <done>
    - `href="#process"` appears on the Process nav link
    - `id="process"` is on the process section element
    - Section heading h2 reads "From first conversation to shipped product."
    - Eyebrow reads "How we work"
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace 4-step timeline with 5-step founder-POV timeline + update CSS grid</name>
  <read_first>
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html (read before editing — confirm current timeline block boundaries)
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css (read before editing — confirm line 779 grid rule)
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/04-trust-conversion/04-UI-SPEC.md (Copywriting Contract — Process Section table for exact step copy)
  </read_first>
  <files>index.html, styles.css</files>
  <action>
**In index.html:** Replace the entire `<div class="timeline">` block (currently 4 articles) with the following 5-article version using exact copy from the Copywriting Contract. All 5 articles use `class="timeline-step reveal"` — no new classes:

```html
<div class="timeline">
  <article class="timeline-step reveal">
    <span>01</span>
    <h3>Discovery</h3>
    <p>We start with a focused async brief — you fill in a short doc, I dig into your stack and goals. No 90-minute intro calls. Within 48 hours I'll have read everything and come back with the right questions.</p>
  </article>
  <article class="timeline-step reveal">
    <span>02</span>
    <h3>Scope</h3>
    <p>I map the exact deliverables, timeline, and tech choices. You get a clear written scope — no surprises mid-build. If anything changes, we adjust the scope in writing before touching code.</p>
  </article>
  <article class="timeline-step reveal">
    <span>03</span>
    <h3>Build</h3>
    <p>I build in short, shippable increments. You get a weekly Loom walkthrough of what's done and what's next — so you're never guessing where the project stands. No waiting for a big reveal.</p>
  </article>
  <article class="timeline-step reveal">
    <span>04</span>
    <h3>Ship</h3>
    <p>Production deployment, handoff documentation, and a recorded walkthrough you keep forever. If you have a dev team, I make sure they can own it. If you don't, I make sure you can.</p>
  </article>
  <article class="timeline-step reveal">
    <span>05</span>
    <h3>Iterate</h3>
    <p>Most projects improve fastest in the first weeks after launch. I stay engaged — monitoring for issues, acting on real usage data, and extending what's working. This is a product partnership, not a project handoff.</p>
  </article>
</div>
```

**In styles.css:** Find the `.timeline` rule (currently at line ~779):
```css
.timeline {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 16px;
}
```

Change `repeat(4, minmax(0, 1fr))` to `repeat(5, minmax(0, 1fr))`:
```css
.timeline {
  display: grid;
  grid-template-columns: repeat(5, minmax(0, 1fr));
  gap: 16px;
}
```

The 940px and 640px media query breakpoints already collapse `.timeline` to 2fr and 1fr respectively — those rules do NOT need changes.

No new CSS classes are introduced. The 5th article inherits all `.timeline-step` styles.
  </action>
  <verify>
    <automated>grep -c 'class="timeline-step reveal"' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html && grep -n 'repeat(5, minmax(0, 1fr))' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css && grep -n 'Iterate' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html && grep -n 'weekly Loom' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html</automated>
  </verify>
  <done>
    - `grep -c 'class="timeline-step reveal"'` returns 5
    - `styles.css` contains `repeat(5, minmax(0, 1fr))` in the `.timeline` rule
    - `index.html` contains "Iterate" as step 05 heading
    - `index.html` contains "weekly Loom" in the Build step description (async-first signal)
    - `index.html` contains "Discovery", "Scope", "Build", "Ship", "Iterate" headings inside `.timeline`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Plan 01 is pure HTML/CSS static content. No user input, no data fetch, no external calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | index.html process section copy | accept | Static HTML, no runtime mutation. Content is hardcoded and served via Netlify CDN. |
</threat_model>

<verification>
After both tasks complete:

1. Open the site in a browser. The nav "Process" link scrolls to the process section.
2. The process section shows the heading "From first conversation to shipped product." with eyebrow "How we work".
3. Five timeline cards are visible at desktop, in a single row of 5 columns.
4. Steps are labeled Discovery, Scope, Build, Ship, Iterate with numbers 01–05.
5. The Build step description mentions "weekly Loom".
6. At viewport ≤940px the timeline collapses to 2 columns; at ≤640px to 1 column.
</verification>

<success_criteria>
- `index.html` has `id="process"` on the process section element
- Nav contains `href="#process"` for the Process link
- Process section h2 = "From first conversation to shipped product."
- Process section eyebrow = "How we work"
- Exactly 5 `<article class="timeline-step reveal">` elements inside `.timeline`
- Step 03 (Build) `<p>` contains "weekly Loom"
- Step 05 (Iterate) `<h3>` contains "Iterate"
- `styles.css` `.timeline` rule has `repeat(5, minmax(0, 1fr))`
- No inline hex colors introduced (CSS token variables only)
</success_criteria>

<output>
After completion, create `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/04-trust-conversion/04-01-SUMMARY.md` documenting:
- What was changed (process section id, heading copy, 4→5 steps, CSS grid update, nav href)
- Exact line numbers of changes made
- Any deviations from this plan
</output>
