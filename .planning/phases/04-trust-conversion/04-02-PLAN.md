---
phase: 04-trust-conversion
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - index.html
  - styles.css
autonomous: true
requirements:
  - ABOU-01
  - ABOU-02
  - ABOU-03
  - ABOU-04

must_haves:
  truths:
    - "An About section exists in the DOM between #portfolio and #process"
    - "About section has id='about' so the nav About link resolves correctly"
    - "Left column contains two bio paragraphs plus GitHub and LinkedIn links"
    - "Right column contains a credentials-card with 4 credential lines"
    - "Bio paragraph 2 contains the 'no account managers' differentiator (ABOU-02)"
    - "Nav About link href points to #about (not #contact)"
    - "credentials-card has NO backdrop-filter — iOS Safari safe"
    - "about-grid collapses to single column at max-width: 940px"
  artifacts:
    - path: "index.html"
      provides: "About section with correct id, split layout, bio, credentials-card"
      contains: "id=\"about\""
    - path: "styles.css"
      provides: "about-section, about-grid, about-bio, about-links, about-link, credentials-card, credentials-line CSS"
      contains: ".credentials-card"
  key_links:
    - from: "nav 'About' link"
      to: "#about"
      via: "href attribute on nav-link"
      pattern: "href=\"#about\""
    - from: ".about-grid"
      to: "two columns (bio + credentials-card)"
      via: "grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr)"
      pattern: "minmax\\(0, 1\\.1fr\\)"
    - from: ".credentials-card"
      to: "spotlight JS handler"
      via: "class selector added in Plan 03"
      pattern: "credentials-card"
---

<objective>
Insert a new About section (`<section id="about" class="section about-section">`) into index.html between the `#portfolio` section and the `#process` section. Add all About CSS to styles.css (after the `.portfolio-section` block). Fix the nav "About" link href from `#contact` to `#about`.

Purpose: Delivers ABOU-01 (first-person bio with credibility anchor), ABOU-02 ("no account managers" differentiator), ABOU-03 (GitHub + LinkedIn links), ABOU-04 (split layout). Creates the credentials-card that Plan 03 will add to the spotlight selector.

Output: Updated index.html (About section inserted, nav href fixed), updated styles.css (About CSS block appended after portfolio CSS).
</objective>

<execution_context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/04-trust-conversion/04-UI-SPEC.md
</execution_context>

<context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/ROADMAP.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/STATE.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/04-trust-conversion/04-01-SUMMARY.md

<interfaces>
<!-- Current DOM structure after Plan 01 — key insertion points. -->

index.html DOM order (post Plan 01):
```
<section class="section portfolio-section" id="portfolio"> ... </section>
<!-- INSERT ABOUT SECTION HERE -->
<section class="section process-section" id="process"> ... </section>   ← Plan 01 added id="process"
<section class="contact-section section-dark" id="contact"> ... </section>
<footer class="site-footer"> ... </footer>
```

index.html line 35 (About nav link — still points to #contact after Plan 01):
```html
<a class="nav-link" href="#contact">About</a>
```

styles.css structure — insertion point for About CSS:
After the `.portfolio-section` block (lines ~754–775), before the `.timeline` rule (~line 777).
The About CSS block goes between `.portfolio-section .section-heading p` and `.timeline`.

iOS Safari constraint (from CLAUDE.md):
- `.credentials-card` must NOT have `backdrop-filter` — only `box-shadow` is allowed
- `.reveal` class applies `opacity:0` + `transform:translateY(20px)`. `.credentials-card` has NO `backdrop-filter`, so combining `reveal` and `.credentials-card` is safe.

Existing tokens (do NOT use inline hex):
- `var(--color-surface-1)` = #111113 (about-section bg)
- `var(--color-surface-2)` = #18181b (credentials-card bg)
- `var(--color-accent)` = #7c5cfc (border-left stripe on credentials-card)
- `var(--color-accent-hover)` = #9b7fff (about-link hover)
- `var(--color-border)` = rgba(255,255,255,0.08)
- `var(--color-border-strong)` = rgba(255,255,255,0.14)
- `var(--color-text-secondary)` = #a1a1aa
- `var(--color-text-primary)` = #f4f4f5
- `var(--color-text-muted)` = rgba(255,255,255,0.50)
- `var(--radius-md)` = 8px
- `var(--shadow-card)` = existing card shadow
- `var(--space-4)` = 16px, `var(--space-6)` = 24px, `var(--space-8)` = 32px
- `var(--duration-fast)` = 150ms, `var(--ease-out)` = cubic-bezier(0.16,1,0.3,1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert About section HTML + fix nav About href</name>
  <read_first>
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html (read in full — confirm exact closing tag of #portfolio section and opening tag of #process section to locate insertion point)
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/04-trust-conversion/04-UI-SPEC.md (About Section Skeleton + Copywriting Contract — About Section table for exact copy)
  </read_first>
  <files>index.html</files>
  <action>
Make two changes to index.html:

**Change 1 — Fix nav About href (line ~35):**
Change: `<a class="nav-link" href="#contact">About</a>`
To: `<a class="nav-link" href="#about">About</a>`

**Change 2 — Insert About section between #portfolio closing tag and #process opening tag.**

Locate the line `</section>` that closes `#portfolio` (after the 4th `</article>` of the portfolio-grid), and the line `<section class="section process-section" id="process">` that opens the process section. Insert the following HTML block between them (after the `</section>` that closes #portfolio, before the `<section` that opens #process):

```html
      <section id="about" class="section about-section">
        <div class="section-heading reveal">
          <p class="eyebrow">About</p>
          <h2>You work directly with me.</h2>
        </div>
        <div class="about-grid">
          <!-- Left: bio + social links -->
          <div class="about-bio reveal">
            <p>I'm Manik — an AI engineer and automation builder with 5+ years building AI systems and automation pipelines for startups and growing businesses. I work across the full stack: agent orchestration, LLM integration, DevOps, and the last-mile integrations that actually get adopted.</p>
            <p>When you hire me, there are no account managers, no junior devs handling your project, and no one translating your requirements across three time zones. You get direct access to the person writing the code.</p>
            <div class="about-links">
              <a href="https://github.com/manik-malhotra" target="_blank" rel="noreferrer" class="about-link">GitHub</a>
              <a href="https://www.linkedin.com/in/manik-malhotra-9478617b/" target="_blank" rel="noreferrer" class="about-link">LinkedIn</a>
            </div>
          </div>
          <!-- Right: credentials block — drop-in for photo in v2 -->
          <div class="credentials-card reveal">
            <p class="credentials-line">5+ years in AI engineering</p>
            <p class="credentials-line">Fintech, SaaS, retail &amp; e-commerce</p>
            <p class="credentials-line">Claude, GPT-4, LangChain, OpenAI</p>
            <p class="credentials-line">Docker, GitHub Actions, Netlify, Zapier</p>
          </div>
        </div>
      </section>
```

Indentation: match the surrounding sections (6 spaces for `<section>`, 8 for its children).

Do NOT modify the process section or the contact section.
  </action>
  <verify>
    <automated>grep -n 'id="about"' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html && grep -n 'href="#about"' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html && grep -n 'credentials-card' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html && grep -n 'no account managers' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html && grep -c 'credentials-line' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html</automated>
  </verify>
  <done>
    - `id="about"` exists on the new section element
    - `href="#about"` exists on the About nav link
    - `class="credentials-card reveal"` div is present
    - "no account managers" text appears in bio paragraph 2
    - 4 `credentials-line` elements are present (grep -c returns 4)
    - GitHub link href is `https://github.com/manik-malhotra`
    - LinkedIn link href is `https://www.linkedin.com/in/manik-malhotra-9478617b/`
  </done>
</task>

<task type="auto">
  <name>Task 2: Add About CSS to styles.css</name>
  <read_first>
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css (read in full — locate the end of the .portfolio-section block to find exact insertion point; confirm no .about-section class already exists)
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/04-trust-conversion/04-UI-SPEC.md (About Section Skeleton — CSS contract block; Interaction States table for .about-link and .credentials-card hover behavior)
  </read_first>
  <files>styles.css</files>
  <action>
In styles.css, insert the following CSS block AFTER the `.portfolio-card span` rule block (which ends around line 775) and BEFORE the `.timeline` rule block. Add a section comment to mark the boundary:

```css
/* === ABOUT === */

.about-section {
  background: var(--color-surface-1);
}

.about-grid {
  display: grid;
  grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
  gap: var(--space-8);
  align-items: start;
}

.about-bio p {
  color: var(--color-text-secondary);
  font-size: 1.12rem;
  margin: 0 0 var(--space-4);
}

.about-links {
  display: flex;
  gap: var(--space-4);
  margin-top: var(--space-6);
}

.about-link {
  color: var(--color-text-secondary);
  font-weight: 600;
  font-size: 0.875rem;
  border-bottom: 1px solid var(--color-border-strong);
  padding-bottom: 2px;
  transition: color var(--duration-fast) var(--ease-out), border-color var(--duration-fast) var(--ease-out);
}

.about-link:hover {
  color: var(--color-accent-hover);
  border-color: var(--color-accent);
}

.credentials-card {
  background: var(--color-surface-2);
  border: 1px solid var(--color-border);
  border-left: 3px solid var(--color-accent);
  border-radius: var(--radius-md);
  padding: var(--space-6);
  box-shadow: var(--shadow-card);
}

.credentials-line {
  margin: 0 0 var(--space-4);
  color: var(--color-text-primary);
  font-weight: 700;
  font-size: 1rem;
}

.credentials-line:last-child {
  margin-bottom: 0;
}
```

Then, inside the EXISTING `@media (max-width: 940px)` block (currently at line ~860), add a rule to collapse `.about-grid` to a single column. Find the closing `}` of that media query and insert BEFORE it:

```css
  .about-grid {
    grid-template-columns: 1fr;
  }
```

IMPORTANT constraints:
- `.credentials-card` must NOT have `backdrop-filter` or `-webkit-backdrop-filter` — only `box-shadow` is used (iOS Safari constraint)
- `.about-link:hover` must NOT have `transform` — only color and border-color transitions
- All values use CSS token variables — no inline hex
  </action>
  <verify>
    <automated>grep -n '\.about-section' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css && grep -n '\.credentials-card' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css && grep -n 'minmax(0, 1\.1fr)' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css && grep -n '\.credentials-line' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css</automated>
  </verify>
  <done>
    - `.about-section` rule exists with `background: var(--color-surface-1)`
    - `.about-grid` rule exists with `grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr)`
    - `.credentials-card` rule exists with `border-left: 3px solid var(--color-accent)` and `box-shadow: var(--shadow-card)` — NO `backdrop-filter` present
    - `.credentials-line` rule exists with `font-weight: 700`
    - `.about-link:hover` rule exists — NO `transform` property present
    - The 940px media query block contains `.about-grid { grid-template-columns: 1fr; }`
    - No inline hex values appear in any of the new rules
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External links | GitHub and LinkedIn links open in _blank with rel="noreferrer" — no referrer leak, no opener access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | GitHub/LinkedIn external links | mitigate | `target="_blank" rel="noreferrer"` on both links prevents window.opener access and referrer header leakage — already included in HTML skeleton |
| T-04-02-02 | Tampering | Static About HTML | accept | Content is hardcoded static HTML served via Netlify CDN. No runtime mutation surface. |
</threat_model>

<verification>
After both tasks complete:

1. Open the site in a browser. The nav "About" link scrolls to the About section.
2. The About section appears between the Portfolio and Process sections in the page flow.
3. Left column shows two bio paragraphs; the second mentions "no account managers".
4. GitHub and LinkedIn links are visible and styled with bottom-border underline.
5. Right column shows the credentials-card with 4 bold credential lines and a violet left border stripe.
6. At viewport ≤940px, the two-column layout collapses to a single column (bio above, credentials below).
7. Hovering the credentials-card shows NO backdrop-filter flicker on iOS Safari (box-shadow only).
</verification>

<success_criteria>
- `index.html` has `<section id="about" class="section about-section">` positioned after `#portfolio` closing tag and before `#process` opening tag
- Nav contains `href="#about"` for the About link
- About section has `.section-heading reveal` with eyebrow "About" and h2 "You work directly with me."
- `.about-bio` has 2 `<p>` tags; p2 contains "no account managers"
- `.about-links` has 2 `<a class="about-link">` elements (GitHub, LinkedIn) with correct hrefs and `rel="noreferrer"`
- `.credentials-card` has 4 `.credentials-line` elements with exact text from Copywriting Contract
- `styles.css` contains `.credentials-card` rule with `border-left: 3px solid var(--color-accent)` and NO `backdrop-filter`
- `styles.css` 940px media query contains `.about-grid { grid-template-columns: 1fr; }`
- No inline hex values in new CSS
</success_criteria>

<output>
After completion, create `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/04-trust-conversion/04-02-SUMMARY.md` documenting:
- About section insertion point (before/after which lines)
- Nav href fix confirmed
- CSS insertion point (before/after which lines)
- Any deviations from this plan
</output>
