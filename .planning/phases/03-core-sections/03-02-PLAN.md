---
phase: 03-core-sections
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - styles.css
autonomous: true
requirements:
  - STRIP-01
  - SERV-01
  - SERV-02
  - SERV-03

must_haves:
  truths:
    - "Stale CSS classes for deleted sections are gone from styles.css"
    - ".timeline-step and .portfolio-card rules survive the compound-selector surgery"
    - "New .service-card, .services-section, .service-grid CSS is present"
    - "Logo strip pills render as inline-flex with SVG icon support"
    - "Marquee animation duration is 28s (updated from 18s for 10 pills)"
    - "Responsive breakpoints include .service-grid alongside .portfolio-grid and .timeline"
  artifacts:
    - path: "styles.css"
      provides: "Cleaned shared rules, new services CSS block, updated logo-strip span rule"
      contains: ".service-card {"
    - path: "styles.css"
      provides: "Responsive service-grid rules"
      contains: ".service-grid"
  key_links:
    - from: "styles.css .service-card"
      to: "index.html .service-card articles"
      via: "CSS class selector"
      pattern: ".service-card {"
    - from: "styles.css .timeline-step"
      to: "index.html process section timeline steps"
      via: "CSS class selector (must survive Phase 3 CSS edits)"
      pattern: ".timeline-step {"
---

<objective>
Surgically clean styles.css: remove all CSS rules belonging to the four deleted sections, preserve the shared rules that .timeline-step and .portfolio-card depend on, and add the new CSS for the services section. Also update the marquee duration for 10 pills and extend the logo-strip span rule for inline SVG support.

Purpose: Creates the CSS foundation that Plan 03 relies on. Service cards must have their styles ready before the HTML that uses them is written. The cleanup also prevents orphaned CSS from confusing future maintainers.
Output: styles.css with stale classes removed, shared rules repaired, and new services section CSS added.
</objective>

<execution_context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/PROJECT.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/03-core-sections/03-CONTEXT.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/03-core-sections/03-RESEARCH.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/03-core-sections/03-PATTERNS.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/03-core-sections/03-01-SUMMARY.md

<interfaces>
<!-- Key CSS blocks that are edited or preserved. Extracted from codebase. -->

From styles.css lines 568–572 (marquee animation duration — change 18s to 28s):
```css
.marquee-track {
  display: inline-flex;
  gap: 10px;
  padding: 0 5px;
  animation: marquee 18s linear infinite;  /* ← change to 28s */
  will-change: transform;
}
```

From styles.css lines 605–612 (logo-strip span — add display/align/gap):
```css
.logo-strip span {
  padding: 10px 14px;
  color: var(--color-text-secondary);
  background: var(--color-surface-2);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  font-weight: 700;
}
```

From styles.css lines 619–625 (grid rule — compound selector, surgical edit):
```css
.agent-grid,
.portfolio-grid,
.devops-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
}
```

From styles.css lines 627–638 (base card rule — compound selector, surgical edit):
```css
.agent-card,
.devops-card,
.mini-card,
.timeline-step {
  min-height: 220px;
  padding: 24px;
  background: var(--color-surface-2);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
  transition: transform 180ms ease, border-color 180ms ease, box-shadow 180ms ease;
}
```

From styles.css lines 640–652 (hover rule — compound selector, surgical edit):
```css
.agent-card:hover,
.devops-card:hover,
.mini-card:hover,
.timeline-step:hover,
.portfolio-card:hover {
  transform: translateY(-5px);
  border-color: transparent;
  box-shadow:
    0 0 0 1px var(--color-accent),
    0 0 20px rgba(124, 92, 252, 0.35),
    0 0 60px rgba(124, 92, 252, 0.12),
    var(--shadow-card);
}
```

From styles.css lines 654–661 (span rule — compound selector, surgical edit):
```css
.agent-card span,
.portfolio-card span,
.timeline-step span {
  display: inline-flex;
  margin-bottom: 28px;
  color: var(--color-cyan);
  font-weight: 900;
}
```

From styles.css lines 663–669 (paragraph rule — compound selector, surgical edit):
```css
.agent-card p,
.devops-card p,
.mini-card p,
.timeline-step p,
.portfolio-card p {
  color: var(--color-text-secondary);
}
```

From styles.css lines 711–740 (wide-card + portfolio-card shared rules — surgical edit):
```css
.wide-card,
.portfolio-card {
  overflow: hidden;
  background: var(--color-surface-2);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
}
/* ...other .wide-card rules... */
.wide-card img,
.portfolio-card img { ... }
.wide-card div,
.portfolio-card div { ... }
```

From styles.css lines 885–907 (940px responsive block — surgical edit):
```css
.hero-grid,
.split-section,
.contact-section,
.wide-card {
  grid-template-columns: 1fr;
}
/* ... */
.agent-grid,
.portfolio-grid,
.devops-grid,
.timeline,
.social-grid {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}
/* ... */
.wide-card {
  grid-column: 1 / -1;
}
```

From styles.css lines 909–948 (640px responsive block — surgical edit):
```css
.section,
.split-section,
.contact-section {
  padding: 72px 18px;
}
/* ... */
.agent-grid,
.portfolio-grid,
.devops-grid,
.timeline,
.social-grid {
  grid-template-columns: 1fr;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgical compound-selector cleanup and marquee/logo-strip updates</name>
  <read_first>
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css (read lines 560–780 to confirm all current rule content before editing — line numbers may have shifted slightly from prior edits)
  </read_first>
  <files>styles.css</files>
  <action>
Make these independent edits to styles.css. Read the file first to confirm exact current line numbers. Make each change as a targeted edit.

**Edit 1 — Marquee duration (in .marquee-track rule):**
Change `animation: marquee 18s linear infinite;` to `animation: marquee 28s linear infinite;`

**Edit 2 — Logo-strip span rule: add inline-flex layout for SVG icons.**
Replace the existing `.logo-strip span` rule with:
```css
.logo-strip span {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  padding: 10px 14px;
  color: var(--color-text-secondary);
  background: var(--color-surface-2);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  font-weight: 700;
}
```
Add the following rule immediately after the `.logo-strip span` rule:
```css
.logo-strip svg {
  width: 16px;
  height: 16px;
  fill: currentColor;
  flex-shrink: 0;
}
```

**Edit 3 — Grid rule (compound selector):**
Replace the `.agent-grid, .portfolio-grid, .devops-grid` rule with:
```css
.portfolio-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}
```
(Remove `.agent-grid,` and `.devops-grid,` selectors. Also change column count from `repeat(3, ...)` to `repeat(2, ...)` for a balanced 2+2 layout with 4 cards.)

**Edit 4 — Base card rule (compound selector):**
Remove `.agent-card,`, `.devops-card,`, and `.mini-card,` from the selector. Keep `.timeline-step` as the sole selector. The rule body (min-height, padding, background, border, border-radius, box-shadow, transition) stays EXACTLY as-is:
```css
.timeline-step {
  min-height: 220px;
  padding: 24px;
  background: var(--color-surface-2);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
  transition: transform 180ms ease, border-color 180ms ease, box-shadow 180ms ease;
}
```

**Edit 5 — Hover rule (compound selector):**
Remove `.agent-card:hover,`, `.devops-card:hover,`, `.mini-card:hover,` from the selector. Keep `.timeline-step:hover,` and `.portfolio-card:hover`. Rule body stays EXACTLY as-is:
```css
.timeline-step:hover,
.portfolio-card:hover {
  transform: translateY(-5px);
  border-color: transparent;
  box-shadow:
    0 0 0 1px var(--color-accent),
    0 0 20px rgba(124, 92, 252, 0.35),
    0 0 60px rgba(124, 92, 252, 0.12),
    var(--shadow-card);
}
```

**Edit 6 — Span/label rule (compound selector):**
Remove `.agent-card span,` from the selector. Keep `.portfolio-card span,` and `.timeline-step span`. Rule body stays EXACTLY as-is:
```css
.portfolio-card span,
.timeline-step span {
  display: inline-flex;
  margin-bottom: 28px;
  color: var(--color-cyan);
  font-weight: 900;
}
```

**Edit 7 — Paragraph rule (compound selector):**
Remove `.agent-card p,`, `.devops-card p,`, `.mini-card p,` from the selector. Keep `.timeline-step p,` and `.portfolio-card p`. Rule body stays EXACTLY as-is:
```css
.timeline-step p,
.portfolio-card p {
  color: var(--color-text-secondary);
}
```

**Edit 8 — .wide-card + .portfolio-card shared base rule:**
Remove `.wide-card,` from the selector. Keep `.portfolio-card` as sole selector. Rule body unchanged:
```css
.portfolio-card {
  overflow: hidden;
  background: var(--color-surface-2);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
}
```

**Edit 9 — .wide-card img + .portfolio-card img rule:**
Remove `.wide-card img,` from the selector. Keep `.portfolio-card img` only:
```css
.portfolio-card img {
  width: 100%;
  height: 100%;
  min-height: 280px;
  object-fit: cover;
}
```
(Note: the separate `.portfolio-card img { height: 190px; min-height: 190px; }` rule lower in the file takes precedence for height — do not delete that rule.)

**Edit 10 — .wide-card div + .portfolio-card div rule:**
Remove `.wide-card div,` from the selector. Keep `.portfolio-card div` only:
```css
.portfolio-card div {
  padding: 24px;
}
```

**Delete these standalone blocks entirely (confirm line numbers before deleting):**
- `.split-section { display: grid; grid-template-columns: ...; }` block
- `.image-panel { ... }` block and `.image-panel img { ... }` block
- `.check-list { ... }` block and `.check-list span { ... }` block
- `.social-grid { ... }` block
- `.wide-card { display: grid; ... }` block (the layout rule for .wide-card specifically)
- `.devops-section { background: var(--color-surface-1); }` rule
  </action>
  <verify>
    <automated>grep -c 'agent-card\|devops-card\|mini-card\|split-section\|image-panel\|check-list\|social-grid\|wide-card\|devops-section' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'agent-card' styles.css` returns no matches
    - `grep 'devops-card' styles.css` returns no matches
    - `grep 'mini-card' styles.css` returns no matches
    - `grep '\.split-section' styles.css` returns no matches
    - `grep '\.image-panel' styles.css` returns no matches
    - `grep '\.check-list' styles.css` returns no matches
    - `grep '\.social-grid' styles.css` returns no matches
    - `grep '\.wide-card' styles.css` returns no matches
    - `grep '\.devops-section' styles.css` returns no matches
    - `grep '\.timeline-step' styles.css` returns matches (rule must survive — at minimum the base card rule and hover rule)
    - `grep '\.portfolio-card' styles.css` returns matches (multiple rules must survive)
    - `grep 'marquee 28s' styles.css` returns exactly 1 match (duration updated)
    - `grep 'display: inline-flex' styles.css | grep -c 'logo-strip\|\.logo-strip'` — `.logo-strip span` rule now has `display: inline-flex`
    - `grep '\.logo-strip svg' styles.css` returns exactly 1 match (new rule added)
    - `grep 'repeat(2, minmax(0, 1fr))' styles.css | grep -c 'portfolio-grid'` — portfolio-grid uses 2-column layout
  </acceptance_criteria>
  <done>All stale CSS classes removed. .timeline-step and .portfolio-card rules intact. Marquee duration 28s. Logo-strip span updated for inline SVG. Portfolio-grid is 2-column.</done>
</task>

<task type="auto">
  <name>Task 2: Add new services CSS block and fix responsive rules</name>
  <read_first>
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css (read lines 740–800 to find the insertion point after the portfolio-card block, and read the responsive blocks at the end of the file to find the exact rules to edit — line numbers will have shifted after Task 1 edits)
  </read_first>
  <files>styles.css</files>
  <action>
**Edit 1 — Add new services CSS block.**

Find the line containing `.portfolio-section {` (the block that sets `background: var(--color-bg)`). Insert the new services CSS block immediately BEFORE the `.portfolio-section` rule block. This places services CSS just before the portfolio section CSS in the file, maintaining the top-to-bottom DOM order convention.

Insert this block:
```css
/* === SERVICES === */

.services-section {
  background: var(--color-surface-1);
}

.services-clusters {
  display: grid;
  gap: 56px;
}

.cluster-heading {
  margin-bottom: var(--space-6);
  color: var(--color-text-secondary);
  font-size: 0.82rem;
  font-weight: 800;
  letter-spacing: var(--tracking-label);
  text-transform: uppercase;
}

.service-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
}

.service-card {
  min-height: 220px;
  padding: 28px;
  background: var(--color-surface-2);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
  transition: transform 180ms ease, border-color 180ms ease, box-shadow 180ms ease;
}

.service-card:hover {
  transform: translateY(-5px);
  border-color: transparent;
  box-shadow:
    0 0 0 1px var(--color-accent),
    0 0 20px rgba(124, 92, 252, 0.35),
    0 0 60px rgba(124, 92, 252, 0.12),
    var(--shadow-card);
}

.service-card h3 {
  margin-bottom: var(--space-3);
}

.service-card p {
  color: var(--color-text-secondary);
}
```

**Edit 2 — Fix responsive rule at max-width: 940px (first block to edit):**

Find the rule containing `.hero-grid` and `.split-section` and `.contact-section` and `.wide-card` with `grid-template-columns: 1fr`. Change it to:
```css
.hero-grid,
.contact-section {
  grid-template-columns: 1fr;
}
```
(Remove `.split-section,` and `.wide-card` — those elements are deleted.)

**Edit 3 — Fix responsive rule at max-width: 940px (second block to edit):**

Find the rule containing `.agent-grid`, `.portfolio-grid`, `.devops-grid`, `.timeline`, `.social-grid` with `grid-template-columns: repeat(2, minmax(0, 1fr))`. Replace it with:
```css
.portfolio-grid,
.service-grid,
.timeline {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}
```
(Remove `.agent-grid,`, `.devops-grid,`, `.social-grid,` — add `.service-grid,`.)

**Edit 4 — Delete .wide-card standalone rule at 940px:**

Find and delete the standalone rule `.wide-card { grid-column: 1 / -1; }` inside the `@media (max-width: 940px)` block.

**Edit 5 — Fix responsive rule at max-width: 640px (section padding rule):**

Find the rule containing `.section`, `.split-section`, `.contact-section` with `padding: 72px 18px`. Change it to:
```css
.section,
.contact-section {
  padding: 72px 18px;
}
```
(Remove `.split-section,`.)

**Edit 6 — Fix responsive rule at max-width: 640px (grid columns rule):**

Find the rule containing `.agent-grid`, `.portfolio-grid`, `.devops-grid`, `.timeline`, `.social-grid` with `grid-template-columns: 1fr`. Replace it with:
```css
.portfolio-grid,
.service-grid,
.timeline {
  grid-template-columns: 1fr;
}
```
(Remove `.agent-grid,`, `.devops-grid,`, `.social-grid,` — add `.service-grid,`.)
  </action>
  <verify>
    <automated>grep -n '\.service-card\|\.service-grid\|\.services-section\|\.services-clusters\|\.cluster-heading' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep '\.services-section' styles.css` returns exactly 1 match
    - `grep '\.service-card {' styles.css` returns exactly 1 match
    - `grep '\.service-card:hover' styles.css` returns exactly 1 match
    - `grep '\.service-grid' styles.css` returns at least 3 matches (base rule + 940px media + 640px media)
    - `grep '\.cluster-heading' styles.css` returns exactly 1 match
    - `grep '\.services-clusters' styles.css` returns exactly 1 match
    - `grep 'split-section' styles.css` returns no matches (all references removed)
    - `grep 'wide-card' styles.css` returns no matches (all references removed)
    - `grep 'social-grid' styles.css` returns no matches
    - `grep 'agent-grid' styles.css` returns no matches
    - `grep 'devops-grid' styles.css` returns no matches
    - `grep 'backdrop-filter' styles.css | grep 'service-card'` returns no matches (iOS Safari constraint respected — .service-card must NOT have backdrop-filter)
  </acceptance_criteria>
  <done>New services CSS block added before .portfolio-section. Responsive breakpoints updated: service-grid included at both 940px and 640px. All stale grid/column selectors removed from media query blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static CSS served by Netlify CDN | No dynamic CSS generation; no user-controlled values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | styles.css shared compound selectors | accept | Static file; CSS edits affect visual rendering only. Worst-case: visual regression, not security issue |
| T-03-03 | Denial of Service | iOS Safari transform+backdrop-filter | mitigate | .service-card uses transform on :hover only, no backdrop-filter — confirmed safe per CLAUDE.md constraint |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c 'agent-card\|devops-card\|mini-card\|split-section\|wide-card\|social-grid\|devops-grid\|agent-grid\|image-panel\|check-list\|devops-section' styles.css` outputs `0`
2. `grep -c '\.timeline-step' styles.css` outputs at least 3 (base rule, hover rule, span rule, paragraph rule — must all survive)
3. `grep -c '\.portfolio-card' styles.css` outputs at least 6 (multiple rules must survive)
4. `grep '\.service-card' styles.css` shows the new block with background, border, border-radius, box-shadow, transition
5. `grep 'marquee 28s' styles.css` outputs 1 result
6. `grep '\.logo-strip svg' styles.css` outputs 1 result
</verification>

<success_criteria>
- styles.css contains no references to deleted CSS classes (.agent-card, .devops-card, .mini-card, .split-section, .image-panel, .check-list, .social-grid, .wide-card, .agent-grid, .devops-grid, .devops-section)
- .timeline-step rules intact (base, hover, span, paragraph)
- .portfolio-card rules intact (base, hover, img, div, span, overflow)
- New services CSS block present (.services-section, .services-clusters, .cluster-heading, .service-grid, .service-card, .service-card:hover, .service-card h3, .service-card p)
- .service-grid responsive rules at both 940px and 640px breakpoints
- .logo-strip span has display: inline-flex, align-items: center, gap: var(--space-2)
- .logo-strip svg rule present
- Marquee duration is 28s
</success_criteria>

<output>
After completion, create `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/03-core-sections/03-02-SUMMARY.md`
</output>
