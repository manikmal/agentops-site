---
phase: 03-core-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - script.js
autonomous: true
requirements:
  - SERV-03
  - PORT-01

must_haves:
  truths:
    - "The four old SME sections (agents, automations, social, devops) are gone from the page"
    - "The header nav 'Services' link points to #services and 'Work' points to #portfolio"
    - "Service cards and portfolio cards receive the violet spotlight glow on cursor move"
    - "No stale IDs (#agents, #automations, #devops) remain in index.html"
  artifacts:
    - path: "index.html"
      provides: "Clean HTML between hero and portfolio — old sections removed, nav hrefs corrected"
      contains: "href=\"#services\""
    - path: "script.js"
      provides: "Updated spotlight selector targeting .service-card and .portfolio-card only"
      contains: ".service-card, .portfolio-card"
  key_links:
    - from: "index.html nav"
      to: "#services section"
      via: "href attribute on Services nav-link"
      pattern: "href=\"#services\""
    - from: "script.js spotlight querySelectorAll"
      to: ".service-card elements"
      via: "querySelectorAll selector string"
      pattern: "service-card, .portfolio-card"
---

<objective>
Delete all four old SME HTML sections, fix the four stale nav href values, and update the spotlight JS selector — in a single atomic commit. These three changes must land together: the nav must point to #services the moment those old IDs disappear, and the spotlight selector must include .service-card the moment those cards arrive (which is Plan 03).

Purpose: Creates the clean HTML gap between the hero/tech-strip and the portfolio section where the new services section will be inserted in Plan 03. Prevents the nav active-state from breaking and ensures spotlight is ready for new cards.
Output: index.html with lines 115–217 deleted and nav hrefs corrected. script.js with spotlight selector updated.
</objective>

<execution_context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/PROJECT.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/ROADMAP.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/03-core-sections/03-CONTEXT.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/03-core-sections/03-RESEARCH.md
@/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/03-core-sections/03-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From index.html lines 31–37 (nav block — current state):
```html
<a class="nav-link" href="#agents">Services</a>
<a class="nav-link" href="#automations">Work</a>
<a class="nav-link" href="#devops">Process</a>
<a class="nav-link" href="#portfolio">About</a>
<a class="nav-link" href="#contact">Contact</a>
```

From index.html lines 104–113 (tech strip — NOT touched in this plan):
```html
<section class="logo-strip" aria-label="Systems we connect">
  <div class="marquee-track">
    <span>Google Sheets</span>
    ...
  </div>
</section>
```

From index.html lines 115–217 (DELETE BLOCK — four old SME sections):
- Line 115: `<section class="section" id="agents">`
- Line 154: end of agents section `</section>`
- Line 155: `<section class="split-section section-dark" id="automations">`
- Line 172: end of automations section `</section>`
- Line 173: `<section class="section social-section">`
- Line 197: end of social section `</section>`
- Line 198: `<section class="section devops-section" id="devops">`
- Line 218: end of devops section `</section>`

From index.html line 219 (portfolio section — NOT touched in this plan):
```html
<section class="section portfolio-section" id="portfolio">
```

From script.js line 138 (spotlight selector — current state):
```javascript
document.querySelectorAll(".agent-card, .portfolio-card, .package-card, .devops-card").forEach((card) => {
```

From script.js lines 138–149 (full spotlight block — only line 138 changes):
```javascript
document.querySelectorAll(".agent-card, .portfolio-card, .package-card, .devops-card").forEach((card) => {
  card.addEventListener("pointermove", (event) => {
    const rect = card.getBoundingClientRect();
    const x = event.clientX - rect.left;
    const y = event.clientY - rect.top;
    card.style.backgroundImage = `radial-gradient(circle at ${x}px ${y}px, rgba(124, 92, 252, 0.12), transparent 34%)`;
  });
  card.addEventListener("pointerleave", () => {
    card.style.backgroundImage = "";
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old SME sections and fix nav hrefs in index.html</name>
  <read_first>
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html (read fully before any edit — confirm exact current line numbers for the four sections and nav block)
  </read_first>
  <files>index.html</files>
  <action>
Make two independent edits to index.html:

**Edit 1 — Nav hrefs (lines 32–35):**
Replace the four stale nav-link hrefs with correct anchors:
```html
<!-- BEFORE -->
<a class="nav-link" href="#agents">Services</a>
<a class="nav-link" href="#automations">Work</a>
<a class="nav-link" href="#devops">Process</a>
<a class="nav-link" href="#portfolio">About</a>

<!-- AFTER -->
<a class="nav-link" href="#services">Services</a>
<a class="nav-link" href="#portfolio">Work</a>
<a class="nav-link" href="#contact">Process</a>
<a class="nav-link" href="#contact">About</a>
```
The fifth link (`href="#contact"`) stays untouched.

**Edit 2 — Delete four old SME sections:**
Delete the entire block from the opening `<section class="section" id="agents">` tag through the closing `</section>` of the devops section. This is a contiguous block of approximately lines 115–217 (confirm exact line numbers by reading the file first). After deletion, the line immediately before the portfolio section (`<section class="section portfolio-section" id="portfolio">`) must immediately follow the closing `</section>` of the logo-strip section.

The exact content to delete starts with:
```html
      <section class="section" id="agents">
```
...and ends with:
```html
      </section>

      <section class="section portfolio-section" id="portfolio">
```
Delete everything from `<section class="section" id="agents">` up to (but NOT including) `<section class="section portfolio-section" id="portfolio">`.

After the delete, the DOM order between the logo-strip and portfolio section must be:
```
</section>  ← end of logo-strip

      <section class="section portfolio-section" id="portfolio">
```
(A blank line between is acceptable.)
  </action>
  <verify>
    <automated>grep -n 'href="#agents"\|href="#automations"\|href="#devops"\|id="agents"\|id="automations"\|id="devops"\|class="split-section"\|class="social-section"\|class="devops-section"' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/index.html | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'href="#agents"' index.html` returns no matches
    - `grep 'href="#automations"' index.html` returns no matches
    - `grep 'href="#devops"' index.html` returns no matches
    - `grep 'href="#services"' index.html` returns exactly 1 match (the nav link)
    - `grep 'href="#portfolio"' index.html` returns exactly 1 match (the "Work" nav link)
    - `grep 'id="agents"' index.html` returns no matches
    - `grep 'id="automations"' index.html` returns no matches
    - `grep 'id="devops"' index.html` returns no matches
    - `grep 'class="split-section' index.html` returns no matches
    - `grep 'class="social-section' index.html` returns no matches
    - `grep 'class="devops-section' index.html` returns no matches
    - `grep 'class="agent-card' index.html` returns no matches
    - `grep 'class="devops-card' index.html` returns no matches
    - `grep 'class="mini-card' index.html` returns no matches
    - `grep 'class="portfolio-section' index.html` returns exactly 1 match (portfolio section still present)
    - `grep 'id="portfolio"' index.html` returns exactly 1 match (portfolio section still present)
  </acceptance_criteria>
  <done>All four old SME sections deleted from index.html. Nav hrefs corrected: Services→#services, Work→#portfolio, Process→#contact, About→#contact. Portfolio section still intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update spotlight selector in script.js</name>
  <read_first>
    - /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/script.js (read lines 135–155 to confirm the exact current selector string before editing)
  </read_first>
  <files>script.js</files>
  <action>
On line 138 of script.js, replace the querySelectorAll selector string:

```javascript
// BEFORE (line 138):
document.querySelectorAll(".agent-card, .portfolio-card, .package-card, .devops-card").forEach((card) => {

// AFTER (line 138):
document.querySelectorAll(".service-card, .portfolio-card").forEach((card) => {
```

Only line 138 changes. Lines 139–149 (the event listener body) are NOT touched.

Removed: `.agent-card` (section deleted), `.package-card` (was orphaned — never in HTML), `.devops-card` (section deleted).
Added: `.service-card` (will be added by Plan 03).
Kept: `.portfolio-card` (still present).
  </action>
  <verify>
    <automated>grep -n 'querySelectorAll' /Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/script.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'agent-card' script.js` returns no matches
    - `grep 'devops-card' script.js` returns no matches
    - `grep 'package-card' script.js` returns no matches
    - `grep '".service-card, .portfolio-card"' script.js` returns exactly 1 match
    - `grep 'pointermove' script.js` returns exactly 1 match (event listener body unchanged)
    - `grep 'pointerleave' script.js` returns exactly 1 match (event listener body unchanged)
    - `grep 'rgba(124, 92, 252, 0.12)' script.js` returns exactly 1 match (gradient value unchanged)
  </acceptance_criteria>
  <done>spotlight selector reads `.service-card, .portfolio-card` — the two removed selector strings are gone, .service-card is present, remaining event listener body is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML served by Netlify CDN | No server-side execution; all content is pre-built |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | index.html nav hrefs | accept | Static file; no user-modifiable state. Wrong hrefs cause broken nav UX only — no security impact |
| T-03-02 | Information Disclosure | Deleted section IDs | accept | Removing IDs from HTML is cosmetic; no sensitive data was in those sections |
</threat_model>

<verification>
After both tasks are complete, confirm:
1. `grep -c 'id="agents"\|id="automations"\|id="devops"' index.html` outputs `0`
2. `grep 'href="#services"' index.html` outputs 1 result
3. `grep '".service-card, .portfolio-card"' script.js` outputs 1 result
4. Open index.html in a browser: nav shows Services/Work/Process/About/Contact; scrolling past hero goes directly to the portfolio section (no blank gap for the deleted sections)
</verification>

<success_criteria>
- All four old SME sections removed from index.html
- Nav hrefs: Services→#services, Work→#portfolio, Process→#contact, About→#contact
- script.js spotlight selector: `.service-card, .portfolio-card` only
- Portfolio section, process section, contact section all still intact (not accidentally deleted)
- No stale IDs remain: no `#agents`, `#automations`, `#devops` anywhere in index.html
</success_criteria>

<output>
After completion, create `/Users/manikmalhotra/Documents/Freelancing/sme-ai-devops-site/.planning/phases/03-core-sections/03-01-SUMMARY.md`
</output>
